# 本项目协作与同步

- 默认中文。用户已授权按当前原型范围持续开发，普通实现细节自行处理；真实API、付费、部署和权限改变另按具体授权执行。
- 按coding-workflow建立本轮任务约定、适当验证与独立只读审查。开发检查不代替用户验收或真人试玩。
- 当前为本机四步工作台：真实文件读取/OCR、模型接口、SQLite任务记录和ZIP导出已实现；历史Mock仅作演示与回归。模型小样、整本真实联调、用户验收与真人试玩分别记录，不把历史检查、模拟输出或目录设置说成当前作品已通过验证或已导出。
- `02-演示与测试资料`、`agent-blueprint`、外部原创项目与`/Users/syk/.codex/skills/juben-design`只读，不纳入本仓库。只使用应用自身`src/mocks`中的演示快照。
- 用户要求建立GitHub仓库并随开发更新。当前默认同步范围为代码、结构、Mock、测试与计划，时机为每轮开发完成适用检查与审查之后。不要把尚未回复的选项记成用户已明确选择。
- 完成一轮已授权修改后，检查差异及暂存清单，使用`python3 scripts/sync-github.py --message "本轮修改说明"`提交并推送，核对返回的本地与远程版本。不用每轮重新询问普通代码同步权限。
- 用户本轮明确授权将`w93139/juben-workbench`代码开源；完成发布前审查后可切换该仓库公开。后续仍只同步此指定仓库，不自行改变其他仓库权限。代码MIT许可，原创故事及参考摘录不自动纳入MIT，见LICENSE与NOTICE.md。不要推送其他项目、真实输入材料、数据库、浏览器状态、目录句柄或密钥。新类型数据或新增同步范围先确认用途。
- 每次同步前执行工作区、暂存区和所有本地引用可达历史的凭证与路径检查；公开前还须确认远程引用已完整取得。发现疑似秘密应停发并处理，不通过改名或只删最新版绕过；不要在聊天、日志或公开Issue显示秘密值。API密钥只进入服务端被忽略配置或部署Secret，禁止客户端代码、浏览器存储及NEXT_PUBLIC_敏感变量，后续接入验收见SECURITY.md。
- 不强制推送，不自动重置工作区，不覆盖远程修改。远程领先或网络失败时保留本地修改／提交，说明尚未同步并处理后重试。
- 脚本只负责一次文件版本同步；没有后台监听。用户单独编辑文件后可运行手动入口。GitHub代码版本与浏览器创作数据备份必须分别说明。
