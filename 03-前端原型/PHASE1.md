# 第一阶段：拆解诊断与受控验证

本轮基线：fb5b66f。代码在独立 worktree；不更新现用 3107 服务，不提交或推送远程。

目标：逐次记录分段、合并和最终汇总的安全诊断，硬性限制模型调用次数，提供不外呼、不清缓存的计划检查及默认关闭的单批真实验证入口。

不做：真实付费请求、整本重传/OCR、删除运行数据、调整分段或模型额度、正文/交叉审查重构、旧组件清理。

完成条件：旧任务可读；原缓存身份保持；未知用量保持未知；敏感嵌套字段不进入诊断；次数限制在发送前生效；诊断失败不遮蔽原始错误；dry-run 零模型调用且不清理缓存；真实验证一次发送后停止并有持久防重复记录，计费依据不足时零外呼。

验证：聚焦单元、完整单元、typecheck、lint、隔离生产构建、相关 E2E、仓库扫描及独立只读审查。所有运行数据在隔离副本或临时目录，模型使用假 transport。

风险：原用户浏览器尚未定位，当前可访问 Ego origin 无项目。服务器备份不含原始输入。蚂蚁公开价格结构已改变，max_tokens 计费含义及平台内部重试费用未知，不能将估算当作金额硬上限。真实调用须另有明确输入、计费依据与用户授权。

## 交付状态（2026-09-17，本机）

代码位于独立分支 `codex/phase1-analysis-diagnostics` 的 `剧本改写-诊断修复` 工作区。原工作区干净，3107 仍由原进程 82803 提供服务。本阶段没有付费请求、重新上传/OCR用户资料、清库、部署、远程提交/推送或重启原服务。代码尚未进入现用服务。

改动职责：

- `src/domain/analysis-diagnostics.ts`、`src/server/analysis-observation.ts` 定义安全诊断契约、供应商字段白名单和未知用量处理；不保存原始对象或嵌套错误内容。
- `studio-models.ts` 对正式分段、合并、最终汇总逐次预留并记录，保留结构/引用/不完整响应拒收；默认每任务 64 次，下一次请求前阻断，不新增重试。
- `studio-job-store.ts` 增加独立调用记录表。相同请求不能因换任务编号或重启再次发送；保留旧任务与缓存身份，提供无清理的缓存读取。缓存保存或诊断持久化失败停止后续调用，不遮蔽已取得的模型故障。
- `analysis-plan.ts` 与共享的分段、引用、请求构造函数提供计划；手动入口默认离线，受控模式仅处理一个指定批次。
- 仅同步直接相关的 README 当前参数与验证说明，保留历史记录为历史。

正式任务用量区分 `known` 和 `missingCalls`，不能把已知用量小计理解为总消耗。`calls` 含发送前预留；预留后保存失败/主机中断时实际供应商是否计费可能未知，不能凭记录数推算实际金额。新增防重复记录不自动到期，避免缓存过期后盲目再次付费；本轮不新增清理入口。既有历史任务缺少完整请求身份，无法追溯补造调用明细。

## 证据备份与缺口

备份在仓库外的 `~/.local/share/juben-workbench/backups/phase1-20260917-051340/`，目录 0700、文件 0600。

- `studio-jobs.sqlite`：从实际应用目录中的任务库用 SQLite 在线备份 API 创建，读取 WAL 中已提交内容；不是复制主库或根目录零字节文件。完整性检查 `ok`，11 条任务、25 条摘要。干运行后复核主库 SHA-256 与清单一致。
- `manifest.json`：包含基线提交、实际参数、模型配置状态、任务 UUID/输入指纹和缓存到期快照；不包含密钥。
- `browser-ego-127.0.0.1-3107.json`：已验证可解析，覆盖可访问 Ego origin 的 LocalStorage 与 IndexedDB 检查结果。但该 profile 没有项目索引，IndexedDB 无项目对象存储。原浏览器/profile/origin 尚未定位，**这不是完整创作数据备份**。
- `dry-run-synthetic.txt`：用当前真实配置的只读视图及上述 SQLite 备份执行离线计划，环境故意设置 `STUDIO_LIVE_VERIFY=1` 仍被 `analysis:plan` 强制关闭。样例 10 文档、4 批、分段缓存命中 0；预计新增 5–8 次为保守计划区间，单批实际请求 32221 字节。文件明确标记 synthetic，不能用它替代原失败输入。

没有备份凭据、原始文件及未定位的用户浏览器数据；未改动这些数据。缓存数量、到期时间、进程与价格均为检查时快照，不承诺之后仍有效。

## 验证证据

- `npm test`：36 文件、289 项通过，包括正式编排参数覆盖、length/JSON/结构/引用/HTTP/超时、缺失usage、嵌套数据隔离、次数限制、重启防重复、旧记录/缓存、只读计划、诊断写入失败和主机恢复。
- `npm run typecheck`、`npm run lint`、`npm run build`：通过。
- 相关 E2E：`CI=1 npm run test:e2e -- tests/e2e/studio-recovery.spec.ts tests/e2e/four-stage.spec.ts`，18 项通过；使用隔离3108服务和自有测试资料，模型端点模拟。
- `npm run analysis:plan`：通过，计划可见，默认/强制离线；没有实例化实际任务存储，也没有清理缓存。
- `python3 -B scripts/check-repository.py`、`git diff --check`：通过。
- 独立只读复核：原两项 Important（当前说明参数、本地拒绝原因失真）已修复并复核，未留 Blocker/Important。

测试不连接真实供应商。未验证原始失败输入、蚂蚁实际计费用量/超时行为、真实长剧本成功与内容质量；Mock、构建或单批未来通过都不能代替原路径验收。

## 单次真实验证计划（尚未执行）

目标定位最新失败任务 `d8cdbd21-aecf-4a3e-8fae-0a81cecd38d1` 的第 **1/22** 批。历史记录显示该批 payload 28995 字节、4096输出、约86.6秒后 `finish_reason=length`；更早2048失败、引用/结构/超时另记，不能合并归因。

服务端只有任务指纹/摘要，没有完整原始输入、文档顺序、UUID与原创作要求。当前**不能完整恢复该批**，也不能计算可信的原批请求哈希。下一步须从原浏览器只读取得完整 `documents` 和 `instructions`，先备份，再按历史 `SHA256("analyze" + JSON.stringify(parsedInput))` 与任务指纹核对；相同数据经当前未改动的分段/引用策略应重现22批及首批范围。若不符，停止并标为重建输入；不重新OCR或暗用合成样例。

核对后仅授权以下一次请求：

| 项目 | 固定计划 |
| --- | --- |
| 接口 | `https://maas-api.antdigital.com/v1/chat/completions`，OpenAI兼容 Chat Completions |
| 模型 | `deepseek-v4-pro-0813` |
| 输入 | 经指纹核对的原第1批，引用目录沿用整本原顺序；授权绑定inputFingerprint与精确requestHash |
| 参数 | `max_tokens=4096`，`response_format={"type":"json_object"}`；不发送temperature/reasoning_effort/thinking新参数 |
| 超时与调用 | 240000毫秒，最多1次应用HTTP发送；禁止重试、合并、最终汇总或成功后继续 |
| 验证假设 | 在原参数下取得缺失的usage、finish_reason、请求编号及失败类别，区分输出截断与结构/引用/HTTP/超时；本轮不以增加额度猜测修好 |
| 金额 | 实际金额未知，当前无可靠金额上界，真实入口关闭。建议待账户侧**可核实且足够严格**的本次总限额落实后，再明确授权金额；仅有价格估算不启动 |
| 记录 | 预留文件先独占落盘；完成时保存白名单诊断、输入与请求身份、已知/未知usage及校验路径/代码，不保存正文或推理 |

蚂蚁官方资料核验（2026-09-17）：[快速接入](https://maas.antdigital.com/dt-maas-docs/quick-access)、[公开模型目录](https://maas.antdigital.com/api/v1/model-service/public/page-list?page=1&pageSize=500)、[计费](https://maas.antdigital.com/dt-maas-docs/billing)、[供应商选择](https://maas.antdigital.com/dt-maas-docs/choose-provider)。该模型可用、支持Chat JSON响应，Chat元数据不支持reasoning_effort；不能套用DeepSeek直连接口的thinking开关。目录价格已改为 `priceInfo.prices`，旧flat inPrice/outPrice为空，不能当0。

公开目录中忙时原价输入9元/百万、输出27元/百万，可在重新核对时作为**估算参考**。若请求序列化字节数为B，旧式保守估算为 `((B+2048)*9 + 4096*27)/1,000,000 * 1.2` 元：上述自有样例 B=32221 时约0.503元，**不是原批报价或账单上界**。max_tokens是否包含推理预算、供应商内部重试与实际结算口径尚未明确。平台可能内部重试，代码只能保证应用发送一次。当前单批入口因此要求经人工核实的供应商金额硬限额证据；代码只能校验其绑定、时效与本地一次发送，不能证明外部控制实际生效。

一次结果的处理：`stop`且结构/引用通过，只标该批通过并停止；`length`保存明确返回的用量与finish_reason，若推理用量缺失仍记未知，先核对供应商输出预算口径；HTTP/超时按请求编号核对服务侧接收/计费；JSON/字段/引用失败保留安全路径后另拟针对性方案。任何分支均不自动追加付费请求。

授权仍需绑定恢复后的真实输入、该精确请求和已核对的金额上限，10分钟内有效。本阶段未生成授权文件。缺少原浏览器或可靠限额证据时，不应仅凭“同意调用”绕过这两个前置条件。

## 回退

当前服务仍使用原工作区，停止采用此独立分支即可回退代码，没有必要恢复数据库或浏览器备份。本轮只新增本地提交，不推送。

以后若合入：先检查用户后续差异，再对本轮提交做定向 revert；不要 `reset --hard`，不要用旧备份覆盖现用runtime-data。新增诊断字段向前兼容旧任务，但旧代码的strict任务schema不能直接解析新任务字段；如需回退已运行的新版本，应保留诊断读取兼容补丁，或在单独目录恢复一致性备份并选择旧版只读核对，保留现用库及WAL供恢复。不要在原库上删除诊断/缓存来迁就旧版。

本阶段已完成可观测性和验证准备，尚未证明原始拆解故障已解决。
