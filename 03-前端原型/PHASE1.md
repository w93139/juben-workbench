# 第一阶段：拆解诊断与受控验证

第一阶段实施基线：fb5b66f。代码在独立 worktree；当时未更新现用 3107 服务、未推送远程。后续 GitHub 同步见本文末尾。

目标：逐次记录分段、合并和最终汇总的安全诊断，硬性限制模型调用次数，提供不外呼、不清缓存的计划检查及默认关闭的单批真实验证入口。

不做：真实付费请求、整本重传/OCR、删除运行数据、调整分段或模型额度、正文/交叉审查重构、旧组件清理。

完成条件：旧任务可读；原缓存身份保持；未知用量保持未知；敏感嵌套字段不进入诊断；次数限制在发送前生效；诊断失败不遮蔽原始错误；dry-run 零模型调用且不清理缓存；真实验证一次发送后停止并有持久防重复记录，未选择明确费用方式或缺少授权时零外呼。

验证：聚焦单元、完整单元、typecheck、lint、隔离生产构建、相关 E2E、仓库扫描及独立只读审查。所有运行数据在隔离副本或临时目录，模型使用假 transport。

当前条件见末尾“本轮新增证据与可审核计划”。前两轮的浏览器、计费和运行状态描述为各自检查时快照；本轮已确认相关浏览器空间受用户接管限制，但仍不能读取原输入。服务器备份不含完整原文。金额硬限额不是唯一授权方式，明确接受费用不确定性也必须绑定输入、参数和一次发送。

## 第一阶段交付快照（2026-09-17，代码提交 a9650ed）

代码位于独立分支 `codex/phase1-analysis-diagnostics` 的 `剧本改写-诊断修复` 工作区。原工作区干净，3107 仍由原进程 82803 提供服务。本阶段没有付费请求、重新上传/OCR用户资料、清库、部署、远程提交/推送或重启原服务。代码尚未进入现用服务。

改动职责：

- `src/domain/analysis-diagnostics.ts`、`src/server/analysis-observation.ts` 定义安全诊断契约、供应商字段白名单和未知用量处理；不保存原始对象或嵌套错误内容。
- `studio-models.ts` 对正式分段、合并、最终汇总逐次预留并记录，保留结构/引用/不完整响应拒收；默认每任务 64 次，下一次请求前阻断，不新增重试。
- `studio-job-store.ts` 增加独立调用记录表。相同请求不能因换任务编号或重启再次发送；保留旧任务与缓存身份，提供无清理的缓存读取。缓存保存或诊断持久化失败停止后续调用，不遮蔽已取得的模型故障。
- `analysis-plan.ts` 与共享的分段、引用、请求构造函数提供计划；手动入口默认离线，受控模式仅处理一个指定批次。
- 仅同步直接相关的 README 当前参数与验证说明，保留历史记录为历史。

正式任务用量区分 `known` 和 `missingCalls`，不能把已知用量小计理解为总消耗。`calls` 含发送前预留；预留后保存失败/主机中断时实际供应商是否计费可能未知，不能凭记录数推算实际金额。新增防重复记录不自动到期，避免缓存过期后盲目再次付费；本轮不新增清理入口。既有历史任务缺少完整请求身份，无法追溯补造调用明细。

## 证据备份与缺口

备份在仓库外的 `~/.local/share/juben-workbench/backups/phase1-20260917-051340/`，目录 0700、文件 0600。

- `studio-jobs.sqlite`：从实际应用目录中的任务库用 SQLite 在线备份 API 创建，读取 WAL 中已提交内容；不是复制主库或根目录零字节文件。完整性检查 `ok`，11 条任务、25 条摘要。干运行后复核主库 SHA-256 与清单一致。
- `manifest.json`：包含基线提交、实际参数、模型配置状态、任务 UUID/输入指纹和缓存到期快照；不包含密钥。
- `browser-ego-127.0.0.1-3107.json`：已验证可解析，覆盖可访问 Ego origin 的 LocalStorage 与 IndexedDB 检查结果。但该 profile 没有项目索引，IndexedDB 无项目对象存储。原浏览器/profile/origin 尚未定位，**这不是完整创作数据备份**。
- `dry-run-synthetic.txt`：用当前真实配置的只读视图及上述 SQLite 备份执行离线计划，环境故意设置 `STUDIO_LIVE_VERIFY=1` 仍被 `analysis:plan` 强制关闭。样例 10 文档、4 批、分段缓存命中 0；预计新增 5–8 次为保守计划区间，单批实际请求 32221 字节。文件明确标记 synthetic，不能用它替代原失败输入。

没有备份凭据、原始文件及未定位的用户浏览器数据；未改动这些数据。缓存数量、到期时间、进程与价格均为检查时快照，不承诺之后仍有效。

## 验证证据

- `npm test`：36 文件、289 项通过，包括正式编排参数覆盖、length/JSON/结构/引用/HTTP/超时、缺失usage、嵌套数据隔离、次数限制、重启防重复、旧记录/缓存、只读计划、诊断写入失败和主机恢复。
- `npm run typecheck`、`npm run lint`、`npm run build`：通过。
- 相关 E2E：`CI=1 npm run test:e2e -- tests/e2e/studio-recovery.spec.ts tests/e2e/four-stage.spec.ts`，18 项通过；使用隔离3108服务和自有测试资料，模型端点模拟。
- `npm run analysis:plan`：通过，计划可见，默认/强制离线；没有实例化实际任务存储，也没有清理缓存。
- `python3 -B scripts/check-repository.py`、`git diff --check`：通过。
- 独立只读复核：原两项 Important（当前说明参数、本地拒绝原因失真）已修复并复核，未留 Blocker/Important。

测试不连接真实供应商。未验证原始失败输入、蚂蚁实际计费用量/超时行为、真实长剧本成功与内容质量；Mock、构建或单批未来通过都不能代替原路径验收。

## 原失败批次验证目标（未执行；费用方式以本轮更新为准）

目标定位最新失败任务 `d8cdbd21-aecf-4a3e-8fae-0a81cecd38d1` 的第 **1/22** 批。历史记录显示该批 payload 28995 字节、4096输出、约86.6秒后 `finish_reason=length`；更早2048失败、引用/结构/超时另记，不能合并归因。

服务端只有任务指纹/摘要，没有完整原始输入、文档顺序、UUID与原创作要求。当前**不能完整恢复该批**，也不能计算可信的原批请求哈希。下一步须从原浏览器只读取得完整 `documents` 和 `instructions`，先备份，再按历史 `SHA256("analyze" + JSON.stringify(parsedInput))` 与任务指纹核对；相同数据经当前未改动的分段/引用策略应重现22批及首批范围。若不符，停止并标为重建输入；不重新OCR或暗用合成样例。

核对后仅授权以下一次请求：

| 项目 | 固定计划 |
| --- | --- |
| 接口 | `https://maas-api.antdigital.com/v1/chat/completions`，OpenAI兼容 Chat Completions |
| 模型 | `deepseek-v4-pro-0813` |
| 输入 | 经指纹核对的原第1批，引用目录沿用整本原顺序；授权绑定inputFingerprint与精确requestHash |
| 参数 | `max_tokens=4096`，`response_format={"type":"json_object"}`；不发送temperature/reasoning_effort/thinking新参数 |
| 超时与调用 | 240000毫秒，最多1次应用HTTP发送；禁止重试、合并、最终汇总或成功后继续 |
| 验证假设 | 在原参数下取得缺失的usage、finish_reason、请求编号及失败类别，区分输出截断与结构/引用/HTTP/超时；本轮不以增加额度猜测修好 |
| 金额 | 实际金额未知。可选经核对的供应商限额，或由用户明确接受没有金额硬上限及未知计费的单次请求；估算本身不构成授权或硬限额。凭据轮换未确认，真实入口保持关闭 |
| 记录 | 预留文件先独占落盘；完成时保存白名单诊断、输入与请求身份、已知/未知usage及校验路径/代码，不保存正文或推理 |

蚂蚁官方资料核验（2026-09-17）：[快速接入](https://maas.antdigital.com/dt-maas-docs/quick-access)、[公开模型目录](https://maas.antdigital.com/api/v1/model-service/public/page-list?page=1&pageSize=500)、[计费](https://maas.antdigital.com/dt-maas-docs/billing)、[供应商选择](https://maas.antdigital.com/dt-maas-docs/choose-provider)。该模型可用、支持Chat JSON响应，Chat元数据不支持reasoning_effort；不能套用DeepSeek直连接口的thinking开关。目录价格已改为 `priceInfo.prices`，旧flat inPrice/outPrice为空，不能当0。

公开目录中忙时原价输入9元/百万、输出27元/百万，可在重新核对时作为**估算参考**。若请求序列化字节数为B，旧式保守估算为 `((B+2048)*9 + 4096*27)/1,000,000 * 1.2` 元：上述自有样例 B=32221 时约0.503元，**不是原批报价或账单上界**。max_tokens是否包含推理预算、供应商内部重试与实际结算口径尚未明确。平台可能内部重试，代码只能保证应用发送一次。旧入口只接受供应商金额硬限额证据；本轮依用户要求增加明确接受费用不确定性的方式。两种方式都受绑定、时效与本地一次发送保护；代码不能证明外部控制实际生效。

一次结果的处理：`stop`且结构/引用通过，只标该批通过并停止；`length`保存明确返回的用量与finish_reason，若推理用量缺失仍记未知，先核对供应商输出预算口径；HTTP/超时按请求编号核对服务侧接收/计费；JSON/字段/引用失败保留安全路径后另拟针对性方案。任何分支均不自动追加付费请求。

授权须绑定所选输入、精确请求、连接、凭据轮换确认及明确费用方式，10分钟内有效；本阶段未生成授权文件。原批不能恢复时，可另外审核明确标识的替代样本，不能称为原输入验证。没有硬限额时必须明确接受未知费用，不能将笼统“同意调用”代填为这种选择。

## 回退

当前服务仍使用原工作区，停止采用此独立分支即可回退代码，没有必要恢复数据库或浏览器备份。第一阶段仅新增本地提交；后续分支推送和草稿 PR 不会切换现用服务。

以后若合入：先检查用户后续差异，再对本轮提交做定向 revert；不要 `reset --hard`，不要用旧备份覆盖现用runtime-data。新增诊断字段向前兼容旧任务，但旧代码的strict任务schema不能直接解析新任务字段；如需回退已运行的新版本，应保留诊断读取兼容补丁，或在单独目录恢复一致性备份并选择旧版只读核对，保留现用库及WAL供恢复。不要在原库上删除诊断/缓存来迁就旧版。

本阶段已完成可观测性和验证准备，尚未证明原始拆解故障已解决。


## GitHub 同步与第二阶段准备（2026-09-17 复核）

本次范围：将已完成的第一阶段代码同步至现有仓库及草稿 PR，更新交付链接；核实单批验证前置条件并运行强制离线计划。未增加代码修改、未调用付费模型、未改配置/权限/Secrets、未合并/部署/重启服务。

- 仓库：[w93139/juben-workbench](https://github.com/w93139/juben-workbench)，从 origin 与 GitHub 仓库元数据交叉核实；保持现有公开状态。
- 修复分支：[codex/phase1-analysis-diagnostics](https://github.com/w93139/juben-workbench/tree/codex/phase1-analysis-diagnostics)。
- 草稿 PR：[#1 补齐剧本拆解诊断、调用上限与单批验证入口](https://github.com/w93139/juben-workbench/pull/1)，目标为实际默认分支 `main`，保持 Draft，不自动合并。
- 第一阶段代码：[a9650ede408cb68cd46ed99aa791e5870b454533](https://github.com/w93139/juben-workbench/commit/a9650ede408cb68cd46ed99aa791e5870b454533) 已正常推送。同步前 main 仍为 fb5b66f，本次不向 main 推送；本文后续文档提交不改变该代码版本。

同步入口脚本只允许 main，因此本次按用户明确限定使用显式修复分支 push，未运行会推送 main 的脚本。仍执行同一仓库扫描，覆盖工作区、暂存区和全部本地引用可达历史，未发现规则内凭据或不应上传的文件；提交中没有运行库、备份或剧本原文。扫描不是凭据轮换证明，本轮未读取旧密钥，未验证其轮换状态。

已检查远程 main 与修复分支上的 `.github/workflows/security.yml`，以及实际工作流列表：唯一工作流为 Repository security scan，仅执行只读安全扫描与隔离的同步脚本测试，权限 `contents: read`，没有部署、付费模型调用或应用数据写入步骤。现用 3107 服务仍为原进程82803，没有切换代码。

验证范围与证据：

- 上文289项单元测试、18项相关E2E及typecheck/lint/build对应 **a9650ed 的代码树**。本次核对代码未变、第一阶段记录及本机测试缓存（36个单元文件无失败，E2E最后运行passed），没有为同步重复跑完整测试。缓存只作辅助证据，不当作新的测试运行。
- 本次新执行仓库安全扫描、文档差异检查和强制离线 dry-run；干运行前移除所有外部 `STUDIO_*` 配置，再故意设置 `STUDIO_LIVE_VERIFY=1`，由 `analysis:plan` 强制覆盖为0。没有提供配置目录或读取凭据文件。
- a9650ed 的远程 [push检查](https://github.com/w93139/juben-workbench/actions/runs/35155741416) 和 [PR检查](https://github.com/w93139/juben-workbench/actions/runs/35155863347) 均已完成并成功。其范围是仓库安全扫描和同步脚本测试，不等同于应用单元/E2E或真实供应商验证；后续文档提交的结果以 PR Checks 为准。

原批次恢复与离线计划：

- 通过实际 SQLite 库的只读连接核实，最新失败任务仍为 `d8cdbd21-aecf-4a3e-8fae-0a81cecd38d1`，1/22批、payload28995字节、4096输出、240秒超时；实际库仍11任务、25摘要。未调用会执行过期清理的任务查询接口。
- 已有一致性备份完整性及哈希仍通过；检查时25条摘要未过期。这只是本次快照，不保证后续仍可复用，也没有延长有效期。
- 浏览器清单中发现相关既有工作空间，但工具返回“用户已接管并结束任务，自动化暂停”，要求明确恢复后才能读取。未接管或绕过该控制，因此**原始完整输入与第1批尚未恢复**。最少补充步骤是允许恢复该已有空间的只读访问，再备份 LocalStorage 的项目索引与 IndexedDB `juben-workbench:authoring:v1/projects`，提取未排除文档的原顺序、UUID、name、text和instructions，核对既有任务指纹。无需重新上传或OCR。
- 本次 dry-run 使用明确标记的自有合成样例：10文档、4批、分段缓存命中0、首批序列化请求32221字节；请求哈希 `5e0fb04b5f5aaea79f8450d28d4fd27273811f560981e09e4e260a6bc193ec24`。它只验证离线入口，不是原失败批次，不应据此申请原批调用授权。
- 运行库、其WAL及备份库在离线检查前后哈希一致。仓库外受限记录位于 `~/.local/share/juben-workbench/verification-prep/phase2-20260917-060550/`，包含干运行输出和安全配置状态；不随GitHub同步。

供应商与金额条件历史快照（2026-09-17 06:01–06:02，北京时间；更完整的模型详情核验见末尾）：

- 通过本机服务只返回安全字段的 GET settings 接口确认当前地址仍为蚂蚁 `/v1`，主模型仍为 `deepseek-v4-pro-0813`；只取得配置状态，没有读取或展示密钥值。官方公开目录仍标记 RELEASED、未下架且支持OpenAI Chat协议。
- Chat参数表标记 `response_format=true`、`reasoning_effort=false`；未找到 `json_object` 具体模式保证。`max_tokens` 的通用字段与Chat参数元数据不完整，尚不能证明4096如何映射或是否包含推理量，不能据此改参数。
- 当前公开目录仍采用 `priceInfo.prices`：忙时原价输入9/输出27元每百万Token，活动价6.75/20.25；闲时原价4.50/13.50，活动价3.375/10.125。报价是估算依据，不是本次可靠金额上界；不以缺失flat价格当作0。
- [供应商选择说明](https://maas.antdigital.com/dt-maas-docs/choose-provider) 表明禁用fallback也不等于禁止平台内部失败重试；重试计费、推理计量和客户端超时后的计费边界仍未知。
- 新核实的[空间与权限说明](https://maas.antdigital.com/dt-maas-docs/console/space-management)提供周期Token总量、模型Token总量及Credits限流，但Token额度不等于金额预算，Credits面向预付费。未核实当前账户/Key所属空间及控制是否生效，也没有单请求预扣、在途超额和内部重试扣额度的可靠说明。因此不能称已有人民币硬限额。

该轮同步时的单批入口默认关闭，已接入白名单诊断、240秒超时、精确请求/输入身份核对、一次请求限制和先持久预留再发送。当时缺口为原批输入、供应商计费/额度硬停止依据及凭据轮换确认。本轮已扩展费用授权方式，当前剩余操作与替代方案见下文；不把Token上限或估算冒充金额保护。

## 本轮任务约定：原输入与单次授权准备

用户要求不再把“供应商人民币硬限额”作为唯一可行条件。本轮仅调整手动单批验证入口的授权契约：保留原硬限额方式，另允许用户对精确请求明确接受费用不确定性；两者均要求凭据轮换确认、一次发送、原参数和持久防重复。默认关闭，不生成已批准文件、不读取旧密钥、不执行付费请求。干运行不加载含凭据的配置文件；原输入无法访问时准备一份明确标识的最小替代单批，只证明入口行为，不冒称复现。修改范围限 tests/manual、对应guard测试和直接相关说明；验证使用聚焦离线测试、类型/静态检查、dry-run及独立审查，不重复无关应用测试。


## 本轮新增证据与可审核计划（2026-09-17）

### 原输入访问与替代批次

本轮只再次检查了一次已知 Ego 空间 3「M4 真实资料和蚂蚁联调」（历史相关页面为「人生海海」）。工具仍返回“用户已接管并结束任务”，因此停止访问，未重新接管或改用浏览器磁盘绕过。已向用户请求恢复该空间的只读访问；未收到允许前不会再试。当前无法看到页面，也不能确认界面中恢复控制或完整数据导出的具体控件，不编造按钮。

原第1/22批仍**无法恢复**；不是精确恢复，也不是近似重建。服务端原任务指纹为 `d300646763f58af749cab0c083f00f2c2870b3016ef7acd50d24b8bbeff24293`。恢复后先备份该 origin 的 LocalStorage 与 IndexedDB，再核对完整正文、文档UUID/name、未排除材料顺序、instructions与该指纹，并重算22批及首批payload28995字节。只匹配文件名、大小或批数不足以认定原输入。若浏览器现有内容已变且无对应历史快照，须另列差异，不无限追查或重新OCR。

为避免空等，已在仓库外准备一份**自有合成对照样本**：3份文本，每份3000个UTF-16字符，使用现有测试样例风格并明确标识 synthetic-substitute；不包含用户剧本。它是可单独审核的最小单批候选，**不是原输入，也不是近似重建**。接近原批载荷只便于控制请求规模，不代表内容、信息密度或推理复杂度相同。即使成功，只能证明该样本在该次接口请求下通过，不能排除原批特有问题。

本地受限目录 `~/.local/share/juben-workbench/verification-prep/single-batch-20260917-065508/`：`synthetic-substitute-input.json`、`plan.json`、`dry-run.txt`，目录0700、文件0600；均不进入Git。

| 待审核候选 | 精确范围与参数 |
| --- | --- |
| 输入指纹 | `d63e7f62deec9be27ffb891ba078adf67f59d8c39a9817dfe4ff2cbde81ce1f8` |
| 请求哈希 | `983a6c91d7513ff0fe8cb457d5cd7fab1a49edfe19ec043f51b3fda425570f0d` |
| 输入范围 | 1/1批；fixture-0、fixture-1、fixture-2依此顺序，每份字符范围[0,3000)；该批缓存命中0 |
| 请求规模 | payload28671字节，完整HTTP JSON正文32297字节；原失败payload28995字节，仅比较规模 |
| 请求 | 蚂蚁 `/v1/chat/completions`，`deepseek-v4-pro-0813`，4096输出、json_object、240000毫秒；不增加其他生成参数 |
| 次数与停止 | 手动入口只发1次；原生fetch，无SDK或应用自动重试，成功、截断、校验失败、HTTP错误或超时均停止，不合并、不生成最终汇总 |
| 费用候选 | 忙时原价9/27元每百万输入/输出Token的字节启发式估算，向上取整约0.51元；不是最大金额或已扣费用 |

完整任务计划显示成功情况下还有一次最终汇总（共2次），这是规划信息；本次入口只选择第1批，**不执行那次汇总**。未调整分段大小、模型、输出额度或超时。

### 官方参数与计费证据

北京时间2026-09-17 06:49–06:50重新读取蚂蚁官方公开资料，无认证、无模型调用：

- [该模型专属详情](https://maas.antdigital.com/api/v1/model-service/public/get/modelservice-1786949299916001801)标记已发布、未下架、支持OpenAI Chat；`usageGuide.requestExample`明确展示相同接口、模型与 `max_tokens:4096`，`modelParameters.response_format.description`列出 text/json_object/json_schema。这补上上一轮仅查通用目录的证据缺口，支持保留4096和json_object；**不证明网关实际返回行为或4096包含推理Token**。Chat对reasoning_effort仍标false，不添加直连DeepSeek参数。示例含temperature=0.5但Chat支持表标false，且通用max_tokens元数据不完整，说明文档存在差异；本次不发送temperature，4096示例不能独自证明计费语义。
- [官方价格目录](https://maas.antdigital.com/api/v1/model-service/public/page-list?page=1&pageSize=500)的 `priceInfo.prices`：忙时原价9/27、当前活动价6.75/20.25；闲时原价4.5/13.5、活动价3.375/10.125，单位均为元/百万Token。使用忙时原价估算，不假定活动价适用。目录的旧flat价格空值、详情中的模板示例链接和部分零值规格都不能作为免费或硬限额依据。
- [计费说明](https://maas.antdigital.com/dt-maas-docs/billing)按实际用量、最终账单结算；未明确这次模型的推理计量、失败/超时/客户端取消的计费边界。[供应商选择说明](https://maas.antdigital.com/dt-maas-docs/choose-provider)不保证禁止平台内部失败重试，即使禁用fallback也不能当作“供应商只执行一次”。这几项保持未知，不套用DeepSeek直连规则。
- [空间控制说明](https://maas.antdigital.com/dt-maas-docs/console/space-management)的Token/模型Token额度与预付费Credits不等于已核实的本次人民币硬限额。本轮未登录账户检查或更改额度。

四种概念分别记录：供应商金额硬限额需账户证据，目前未知；应用调用上限为1且代码可执行；输出参数为4096但推理包含关系未知；0.51元是参考价格下的估算。公式为 `ceil((((32297+2048)*9+4096*27)/1000000)*100*1.2)/100` 元；以请求字节近似输入Token，另加2048与20%余量仍无法保证账单上界。240秒仅限制客户端等待，不证明平台停止计算或扣费。正式调用前免费重读报价，并将所选报价与时效写入具体授权，不能复用本次旧时间戳。

### 入口与授权保护变更

改动限于手动验证入口、其保护函数和对应测试。生产 `src`、原分段/缓存身份和现用服务没有改动。执行修复分支的独立Vitest入口即可使用新诊断，不需要重启3107服务。

- 强制dry-run从安装禁止fetch的保护开始，仅使用非敏感模型/地址参数，完全不打开凭据配置；即使给出配置目录也不读取。
- 真实入口要求维护者明确确认旧Key已撤销、新Key已本地配置，且提供审核过的精确请求授权。进入规划前、紧邻凭据读取前和真正发送前均检查时效；缺少确认或过期，零读取/零发送。程序只能验证确认字段，不能替代供应商撤销操作。
- 保留 `provider-cap`，新增 `accepted-uncertainty`：必须明确接受无金额硬上限及推理/平台内部重试/失败超时计费未知，附官方报价、对应实际请求字节的估算。两种方式都绑定输入、请求、连接、4096/240秒/1次和最多10分钟有效期；缺少字段不能默认同意。没有可靠硬限额不再被当作唯一阻塞，但费用不确定性仍需用户针对本次请求接受。
- 保留持久独占预留、不可重复发送、不完整响应拒收、字段和引用校验、安全白名单诊断。实际费用未知为null，不用0.51替代账单，不记录原文/响应对象/推理/凭据。

本轮未生成已批准的授权文件，未读取、显示或使用旧密钥，未发送真实请求。凭据轮换和恢复浏览器的提问尚未收到明确回复；用户消息中的方括号选项不当作确认。

### 验证与接续条件

本轮新增验证：`npx vitest run tests/unit/live-analysis-guard.test.ts tests/unit/analysis-verification-entry.test.ts` 两文件24项通过；`npm run typecheck`、`npm run lint`通过。覆盖两种费用授权、缺少轮换确认、报价/授权过期、请求/连接不符、先预留后发送、重复阻断，以及规划期间过期后的凭据零读取。独立只读审查的两项Important已修复并复核，无剩余Blocker/Important。最终代码再次执行强制离线dry-run：同一请求哈希、1批、零模型请求、零凭据读取，现用主库/WAL及备份哈希前后一致。仓库安全扫描与差异检查通过；聚焦结果及变更代码哈希保存在上述仓库外 `verification-evidence.json`。第一阶段289项单元、18项E2E和build仍只代表a9650ed时的应用代码，不作为新增入口的真实供应商证据。原输入恢复、付费调用和金额实际结算均未验证。

剩余最少操作：允许恢复已知Ego空间的只读访问（先备份，不点拆解），或明确选择上述替代单批；确认旧Key已撤销且新Key已配置。项目代码可确认的本地设置入口为「配置模型」→「API Key」→「更新平台连接」，保存连接不调用模型，勿点击测评。供应商撤销控件未实际看到，仅能指向其API Key管理页面，不能编造具体按钮。请勿在聊天粘贴Key。

条件齐备后再请求对**所选精确输入、相同模型/4096/JSON/240秒、一次请求、所选费用方式、任何结果均停止**的具体授权。成功需finish_reason完整且结构/引用均通过；length/结构/引用/HTTP/超时分别保留安全诊断，用量缺失仍未知，据此另拟下一步，不自动重发。若选替代样本，其结果不证明原失败批次已复现或解决。

回退本轮入口修改可在核对后续用户差异后定向revert本提交；不回滚运行库、备份或浏览器，不删除持久防重复记录。GitHub仍使用上文同一修复分支和草稿PR；不合并、不部署、不改权限、不重启现用服务。
