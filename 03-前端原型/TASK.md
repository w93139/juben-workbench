# Task：阶段B · 参考研究与原创方向

## Current contract · 蚂蚁模型自动选型与 LiteLLM · 2026-09-09

- Goal：用户只提供蚂蚁数科平台地址和 API Key，系统发现可用文本模型，用有金标核对的固定小样完成初筛，在工作台人民币 10 元估算限额内推荐并分配主模型与两路独立审查模型；同时生成后续 LiteLLM 网关所需的无密钥别名配置。
- Non-goals：不把 LiteLLM 当成剧本质量裁判；不凭模型目录名称宣称能力已验证；不在浏览器持久存储、GitHub、日志或前端代码保存密钥；不把工作台估算冒充平台最终账单；本轮不对完整用户剧本做批量付费生成，也不自动启动LiteLLM代理。
- Done when：无项目也能配置蚂蚁平台；密钥只进入受限服务端配置；可读取并筛选账号实际可用且采用简单公开单价的模型；评测开始前显示候选、最大调用数、公开价加安全余量和10元估算限额，未知或复杂计价不能自动运行；任务可停止且不再排队；得分、格式、延迟、用量、费用及分配可恢复；生成三个不同LiteLLM别名且不含密钥。代理自动启动、健康恢复和工作台切换到本机代理列入下一后端批次。
- Scope：模型连接设置、蚂蚁兼容接口适配、金标评测类型与服务、预算账本、LiteLLM无密钥配置与固定依赖、状态界面、PRD/README、安全说明和对应测试。优先复用正式发布的LiteLLM包，不复制其完整源码进仓库。
- Verify：密钥与路径扫描；类型检查、lint、单元测试、生产构建、聚焦浏览器测试；用假服务验证模型发现、未知价格拒绝、预算原子预留/结算、取消、重试和三个不同分配；LiteLLM 配置只做无密钥静态校验；真实模型调用另在 10 元授权内小规模验收；独立只读审查。
- Risks and assumptions：用户授权首次真实评测最多10元。工作台只掌握平台公开价与Token用量，采用8元内部计划阈值、20%单次安全余量和10分钟价格有效期，但仍不能代替平台最终账单；真实启动前建议用户在蚂蚁账号侧设置消费限额。LiteLLM负责后续统一接口、路由和故障处理，本项目负责评测、角色分配、预算和剧本规则。

> 当前任务：A 已获用户验收，本批交付 B 阶段。用户授权在无需额外决定时持续推进开发；普通实现与检查不再逐项申请确认。

## Current contract · 阶段 B · 2026-09-08

- Goal：从本机项目完成“模拟上传／识别 → 校对与保留材料缺口 → 有来源的模拟拆解 → 机制取舍 → 保存原创方向”，刷新后恢复。
- Non-goals：不接真实OCR/AI/后端；不把本机选择的文件冒充已识别，不把机制摘录冒充完整参考本；不修改受保护资料或重审已验证Skill。
- Done when：可载入来源明确的内置研究练习材料，登记本机文件元信息；任务有进度、失败重试、取消与刷新继续；能确认识别问题、排除重复页、明确保留缺失内容；材料变化使旧拆解失效；机制可保留/改造/舍弃并保存理由；人数、题材、时长、体验比例、内容边界和原创思路可保存，风险随目标变化展示；旧A项目能兼容恢复，副本隔离和只读基准仍有效。
- Scope：research领域类型/Mock/Service、四个研究阶段页面、任务状态及阶段进度、数据兼容、测试和PRD/README说明。C—E维持后续开发顺序，开始各批之前补写具体契约；本批不混入其他开发阶段。
- Verify：`npm run typecheck`、`npm run lint`、`npm run test`、`npm run build`、`npm run test:e2e`；新旧数据、失败/取消/过期更新、阶段完整链路、移动端；独立只读审查。
- Assumptions：沿用唯一完整原创样例《名字之外》。OCR异常为明确标识的练习数据；参考拆解使用已有机制研究摘录，客观真相缺失处保持未知。原始参考页面未导入，不捏造阅读覆盖或识别准确率。

## Goal

用户能创建空项目或《名字之外》演示副本，查看完整阶段导航、项目总览和结构数据摘要，编辑项目备注与决定状态，并在刷新后恢复。

## Non-goals

- 不实现真实OCR、模型、后端、账号、队列或部署。
- 不提前实现蓝图编辑器、正文生成、审查修复、试玩表单和导出服务。
- 不修改任何参考资料或Skill；不把历史检查视为当前操作的验证。

## Done when

- 首页／项目列表、新建页面、项目工作台与阶段摘要可访问，桌面与窄屏可用。
- 《名字之外》基准只读；演示副本与空项目能独立保存、编辑和恢复。
- 项目标题／备注和决定状态可更新；编辑不改变只读的原始剧本内容。
- 展示真实结构统计、明确的来源、版本、模拟／历史标识和未试玩状态。
- 持久化有版本校验、存储失败提示、损坏恢复入口；未成功保存不显示成功。
- 不同项目隔离，过期版本写入被拒绝，重复点击有进行中保护。
- 所有业务访问经过Service接口；JSON／Markdown与组件分离。
- 下列验证完成并经过独立只读审查。

## Scope

- 新增：../01-产品需求/03-简易PRD-v0.1.md。
- 应用：当前目录内的app、components、domain、services、mocks、tests与构建配置。
- 更新：当前README（保留原目录用途并增加运行、边界与验证说明）。
- Preserve：../02-演示与测试资料、../agent-blueprint及外部原创项目。

## Verify

- `npm run typecheck` — 类型正确。
- `npm run lint` — 代码检查。
- `npm run test` — Service、数据校验、基准隔离、版本冲突、保存失败与恢复。
- `npm run build` — 生产构建。
- `npm run test:e2e` — 创建／复制／编辑／刷新／阶段导航／存储异常／窄屏。
- 浏览器：检查1440px与390px布局、键盘表单、保存提示、历史与试玩标签。
- 独立reviewer读取任务契约、代码和上述实际结果，修复Blocker／Important并复审。

## Risks and assumptions

- 用户已确认新手优先、仅一套完整示例、正文自由编辑后置。
- 当前是浏览器本机状态，不能跨设备同步，也不提供真实玩家账户隔离。
- 示例版本缺少明确日期时显示“原始样例”，不捏造历史修改日期。
- 保存的项目元信息与底稿分开；底稿只读，只有副本备注／决定状态可修改。
- 依赖版本在当前可用Node.js环境中确认并锁定。

## Verification results · 2026-09-07

- `npm run typecheck`：exit 0。
- `npm run lint`：exit 0，无警告。
- `npm run test`：15 / 15 通过。
- `npm run build`：exit 0，首页、项目列表、新建、项目总览及阶段路由构建成功。
- `npm run test:e2e`：10 / 10 通过（最终运行 9.8 秒）。覆盖契约流程、坏数据备份恢复、拒绝保存、多页面版本冲突、长备注与后台读失败时草稿保留。
- 桌面 1440px、手机 390px：布局人工看图检查完成，手机无横向溢出，浏览器 pageerror 为空。
- 来源核验：11 份源数据哈希、3 份 Markdown 副本与参考目录一致。
- 独立只读审查：Ready，无遗留 Blocker / Important。修复后台读取失败导致草稿卸载的问题；补全明确的冲突覆盖提示及弹窗关闭焦点返回，经过独立浏览器复核。

本次验证是前端功能验证，不是对剧本体验、真实AI或真人试玩的验证。

## Follow-up · 2026-09-08 · 找得到决定与检查入口

- Goal：用户在项目总览和任一阶段的首屏即可找到决定状态及检查记录，无需猜测它们在右侧还是页面下方。
- Non-goals：不推进 B 阶段，不改变剧本数据、历史审查结论、LocalStorage 格式或只读规则。
- Done when：可编辑项目顶部有“修改决定状态”，只读样例有“查看创作决定”及创建副本说明；顶部始终有“检查与试玩”；点击即可打开对应面板，状态选择有明确标签，修改后刷新保留；弹窗可关闭并返回触发按钮焦点。
- Scope：workspace-actions 新组件、workspace / workspace-panels、少量样式、浏览器回归场景及 README 操作说明。复用已有数据与 Service，不添加依赖。
- Verify：先用新增浏览器测试复现入口缺失；修复后运行 `npm run typecheck`、`npm run lint`、`npm run test`、`npm run build`、`npm run test:e2e`，覆盖 1440 / 960 / 390px 与只读／副本／阶段页面；独立只读审查。
- Risks：原位置的面板和弹窗复用同一状态；弹窗中的操作应正确保存且不修改基准。测试在隔离浏览器进行，不动用户本机项目。

### Follow-up verification results

- 复现：新增 960px 测试在旧版本失败，首屏“修改决定状态”入口不存在；代码确认 <=1020px 时右侧区域移至主体内容之后，原始样例的决定只显示标签。
- 修复：项目标题下新增两个常用操作，弹窗复用现有面板；状态下拉框有“修改状态”标签，只读样例说明复制后才能修改。
- 最终 `npm run typecheck` / `npm run lint` / `npm run build`：exit 0；`npm run test`：15 / 15；`npm run test:e2e`：14 / 14（8.0 秒）。
- 新增 4 个浏览器场景：1440 / 960 / 390px 全按钮首屏可见、弹窗修改与刷新保留、阶段页面直接操作、关闭焦点返回、只读复制入口。
- 实际截图查看：960px 首屏入口、390px 决定及检查弹窗均可读。
- 独立只读复审：Ready，无遗留问题；额外验证弹窗保存失败保留旧值、恢复后重试、原面板同步及跨阶段状态，pageerror 为空。
- 预览已在原地址 http://127.0.0.1:3107 重新启动；用户项目、数据格式和受保护资料未改动。


## 阶段 B 验证记录 · 2026-09-08

- 新增独立 research 类型、来源可追溯的JSON、Service操作和四个阶段界面；真实文件只登记元信息，OCR问题是人工练习数据。
- 旧格式1读取不写，首次成功保存升级为格式2；仍使用原存储键。只读样例、其他项目和保护目录不变。
- 类型检查、lint、生产构建通过；单元测试22/22。完整浏览器回归19/19（23.0秒），覆盖A回归、B桌面/390px完整链路、失败重试/取消/刷新继续、文件登记保存失败、多页草稿保护与材料变化失效。
- 桌面1440px、手机390px截图已查看，无横向溢出；完整B浏览器链路pageerror为空。
- 独立审查发现并已修复：未编辑的旧表单可能覆盖他页最新字段。现在无本地修改时同步最新保存，有草稿时保留输入并检查版本冲突；四类研究表单均使用同一保护。
- 已完成但随后失效的拆解任务明确标为“上次完成，当前待重新拆解”，不再展示100%进度误导。
- 独立复审结果：Ready，无遗留Blocker/Important。审查者独立浏览器复核两种跨页编辑顺序，并核对3份摘录、8条机制与来源哈希。
- 原地址 http://127.0.0.1:3107 已启动新生产预览。验证通过不等同于用户已验收B，更不等同于真实OCR/AI或真人试玩验证。

## Follow-up · 2026-09-08 · 找得到参考研究入口

- Goal：用户从原始样例、可编辑项目总览或任一阶段，均可找到“继续参考研究”，顺利进入材料中心并载入练习包。
- Non-goals：不开发C阶段、不改OCR/研究业务或存储格式、不自动创建项目或修改只读样例，不变更受保护资料。
- Done when：项目顶部始终有研究快捷入口；已有项目直达材料中心且保留进度；只读样例点击研究入口或材料页载入操作，明确进入副本创建，创建成功后直达材料中心；未创建时不写本机数据。已载入时明确显示已有状态和下一步。桌面/窄屏、A/B既有功能继续可用。
- Scope：workspace-actions/workspace、research/materials、create-project及新建路由、少量入口说明、README/PRD与浏览器回归。
- Verify：先在旧生产预览复现缺失入口；新增e2e在旧构建失败；修复后运行typecheck/lint/test/build/test:e2e，检查桌面和390px、原始样例/副本/空项目、自动进入材料中心及刷新状态；独立只读复审。
- Risks/assumptions：只能确认当前生产版本的显示条件，无法直接读取用户当前浏览器（CUA管道不可用）；浏览器验证使用隔离Playwright存储，保持预览地址3107与用户数据不变。新建路由只接受固定的research意图，不接受任意跳转地址。


### 参考研究入口修复验证

- 旧生产复现：原始样例总览和蓝图页无“继续参考研究”；只读材料页“载入研究练习包”为禁用按钮。新增1440px入口测试在旧构建失败。
- 最终typecheck、lint、build exit0；单元22/22；浏览器24/24（21.1秒）。1440/960/390px研究入口和创建后载入按钮完整在首屏；新建失败保留输入、重试只创建一份；既有研究状态刷新与跨阶段返回保留。
- 修正原有刷新测试的时序：等待任务保存后出现“取消任务”再刷新，避免刷新抢在重试请求持久化之前。
- 截图已查看；独立只读复审Ready，无遗留问题。审查者在隔离390px浏览器验证只读材料→创建失败→重试成功→载入→返回研究保留，pageerror为空。
- 预览已更新并保持原地址 http://127.0.0.1:3107 ，用户本机项目和所有受保护资料保持不变。

## Follow-up · 多格式材料与文件夹登记 · 2026-09-08

- Goal：整文件夹（含子文件夹）和多种文件可登记；选后先看清数量、分类、相对路径、重复/不支持文件及大文件提示，再批量登记。清单可筛选分页，刷新后保留。
- Non-goals：不接真实OCR、音视频转写、视频抽帧、文件上传、压缩包解压；不读取文件正文或媒体内容，不修改原文件。不开发C阶段。
- Done when：文件/文件夹双入口；保留目录相对路径，不同目录同名文件不互相覆盖；图片/PDF/文档/音频/视频/字幕/压缩包分类及待处理方式明确；单批2000份、单项目5000份作为可配置的原型保护，取消单文件30MB硬限制；大文件可登记但提示后续拆分处理。零字节、隐藏文件、不支持格式、疑似重复在预览列出原因，可登记其余有效项；未提交不写入。存储满/冲突保留待登记清单，失败重试不重复；旧A/B数据无损恢复。
- Scope：独立source-import类型/策略/检查、research Schema与登记Service、source-import组件、材料清单筛选分页、必要存储格式3兼容、测试与PRD/README说明。策略阈值是本产品原型建议，不是第三方OCR能力承诺。
- Verify：typecheck/lint/test/build/test:e2e；真实测试文件夹递归/同名文件/多格式/超10份和超30MB/部分略过/去重/分页/失败重试/旧格式升级/只读隔离；独立只读审查。预览维持3107，隔离浏览器测试。
- Risks/assumptions：只保存元信息，因此文件本身不占浏览器存储，但大量元信息仍可能存储满，必须保留原数据与本次选择。文件夹选择受浏览器/系统文件选择器支持限制，不支持时明确提示多选文件；路径仅保留所选文件夹内相对路径。音视频格式分类基于扩展名，不声称验证了编码可解码；压缩包只登记待解包。重复判断基于相对路径/大小/可用修改时间，无内容哈希，标为疑似重复。

### 多格式材料与文件夹登记验证

- 最终 `npm run typecheck`、`npm run lint`、`npm run build`：exit 0；`npm run test`：31/31；`npm run test:e2e`：27/27（32.6秒）。
- 隔离浏览器实际选择递归测试文件夹：42份中39份可登记，3份明确略过；包含不同目录同名页、300MB PDF、3GB视频、音频、字幕和压缩包。大文件使用稀疏测试文件，只验证元信息登记，不读取或处理内容。
- 覆盖预览不写、取消/取消勾选、失败重试、重复预览、搜索筛选和分页、刷新保留及只读样例；Service验证2000份单批和5000份项目边界。1440px与390px截图已查看，无横向溢出。
- 独立审查发现旧格式中反斜杠/换行文件名会被新输入规则误判损坏。新增回归先复现失败，再分开新导入校验与持久化名称兼容，最终通过；旧格式1/2读取不改存储，成功写入才升级3。
- 独立只读复审：Ready，无遗留Blocker/Important。审查者用隔离浏览器复核旧名称原样保留、跨页冲突、载入最新状态仍保留勾选、重试只登记选中项；浏览器异常为空。
- 新生产预览已恢复原地址 http://127.0.0.1:3107 。本次未接入真实文件处理，未修改用户本机项目、Skill或受保护资料。

## Follow-up · 自动导入进度与输出路径 · 2026-09-08

- Goal：选择文件或文件夹后自动开始模拟上传、展示进度、自动保存并显示结果，无需再点登记或完成；拆解及后续阶段可设置和查看输出位置。
- Non-goals：不接真实上传/OCR/AI，不读取文件正文，不创建本机目录或写出剧本；不开发C—E的主体编辑生成能力，不变更受保护资料。
- Done when：自动分类并略过无效/疑似重复项，保存成功后才能到100%；支持取消、失败后保留清单并重试，数量限制和保存冲突不会造成部分写入；离开页面会取消未完成导入。页面明确标注模拟进度。项目总输出目录与拆解/机制/方向/蓝图/正文/审查/试玩/成品子目录可设置、刷新保留、项目隔离；完整路径预览、未设置提示和只读保护明确；拆解任务记录启动时的计划输出位置，改路径不冒充已移动旧结果。兼容旧项目，读取不改原存储。
- Scope：导入进度事件与Service接口、Mock进度适配、导入组件、输出路径类型/设置组件/Service、拆解任务路径快照、相关测试与README/PRD；复用现有保存/版本冲突策略，不加依赖。
- Verify：typecheck、lint、test、build、test:e2e；浏览器桌面/390px自动完成、混合目录、大文件、取消与失败重试；Service原子保存、冲突和旧格式读取；路径保存/跨阶段/只读/异常输入/失败与冲突、拆解启动快照；独立只读复审。
- Risks/assumptions：用户所说的上传仍属已确认的本地Mock范围，因此进度不表示网络字节传输。输出路径当前只保存计划，浏览器不能凭输入路径直接写本机；未来接后端/授权目录写入时再实际使用。自动导入替代上一轮手动勾选确认；取消只在提交前有效，刷新/离开后需重新选择未导入批次。

### 自动导入与路径设置验证

- 最终typecheck、lint、build：exit 0；单元39/39（8.40秒）；浏览器30/30（22.2秒）。
- 自动导入覆盖递归目录、300MB PDF/3GB视频元信息、无效项和疑似重复自动略过、无确认自动保存、100%仅在保存后、保存失败清单保留、取消与重试、SPA离开取消、只读基准；Service覆盖等待存储锁时取消、跨页冲突及重试不覆盖原记录。
- 输出路径覆盖默认未设置、macOS/Windows拼接、总目录跨阶段共用及子目录独立、刷新保留、旧项目读时不写、只读与项目隔离、非法路径、写入失败和跨页草稿保护；拆解任务启动路径快照不随后续设置改变。
- 1440px与390px浏览器截图已查看，窄屏无横向溢出，相关场景pageerror为空。独立审查指出POSIX目录中的反斜杠可能误触发Windows分隔符；已改为按盘符/UNC前缀判断并补回归。
- 预览已更新并保持 http://127.0.0.1:3107 。用户本机数据未被测试访问，未写真实上传/输出文件，受保护资料保持不变。
- 独立只读复审Ready，无遗留Blocker/Important/Suggestion。独立隔离浏览器确认95%等待锁时离开不提交、跨页冲突后重试仅添加一次且保留另一页备注、旧项目读时不写、路径草稿不静默覆盖；390px无横向溢出，浏览器异常为空。

## Follow-up · 材料下一步与交互选择输出文件夹 · 2026-09-08

- Goal：材料中心顶部下一步进入参考本拆解，不再自跳转；输出路径可通过系统目录选择窗口选取（请求从桌面打开），并保存选中目录和阶段子目录。
- Non-goals：不绕过拆解审计门槛，不接OCR/上传/生成/真实输出，不扫描/修改所选文件夹内容，不建立原生后端，不修改受保护资料。
- Done when：材料页显示“下一步：参考本拆解”，点击正确导航；未审计可浏览下一页但不能开始模拟拆解。其他页的研究入口和只读创建副本保持可用。输出位置提供选择/重新选择文件夹、手动路径；选后预览，保存并刷新保留；取消选择保留原设置，拒绝/不支持/存储失败有可恢复提示，不伪造绝对路径。跨页选择期间发生修改不能静默覆盖，切页后迟到的选择不得写入另一个项目。
- Scope：workspace-actions/workspace、输出设置类型/表单/Service、独立浏览器目录适配器及引用存储、测试、README/PRD。只新增必要的目录引用IndexedDB存储，不加依赖；旧LocalStorage版本保持可读。
- Verify：先用浏览器复现材料中心自跳转；typecheck/lint/test/build/test:e2e；导航1440/390px、审计门槛、目录选择调用参数/取消/失败/不支持/刷新/同名目录引用隔离/旧路径兼容/冲突草稿保护；独立只读复审。
- Risks/assumptions：showDirectoryPicker由用户点击触发、mode=read、startIn=desktop；起始位置由浏览器决定，不保证只可选择桌面目录。浏览器不返回完整本机路径，因此保存目录引用及名称，预览标注“所选文件夹”；不会把名称当作绝对路径。IndexedDB保存引用，实际写入授权和文件生成仍后置；若浏览器不支持目录选择则保留手动输入。浏览器自动化在隔离存储验证适配器边界，不操控用户个人目录。
- 验证环境调整：本机Playwright无痕上下文读取IndexedDB中的OPFS目录引用会导致浏览器退出，纯API最小复现也失败；独立临时profile的完整Chromium可成功往返。目录浏览器场景改用每测试独立临时profile并在结束后关闭删除；不使用用户个人profile，不修改产品逻辑来绕过验证。

### 材料下一步与交互选择文件夹验证

- 旧生产预览复现：材料中心“继续参考研究”的href与点击后URL均为当前materials页，“下一步：参考本拆解”不存在。
- 最终typecheck、lint、build exit 0；单元43/43（8.42秒），完整浏览器36/36（24.9秒）。新增6个浏览器场景与4个Service测试；1440/390px截图已查看。
- 目录场景使用每测试独立临时profile、真实OPFS目录handle和真实IndexedDB往返，替代的仅为系统picker返回值。验证用户点击激活、startIn桌面/read参数、保存与刷新、取消/拒绝/不支持/存储失败、同名引用及跨页草稿保护；没有实际操作系统原生选择窗口。
- 独立只读复审Ready，无遗留Blocker/Important/Suggestion。独立复核目录引用丢失、保存失败恢复、选择期间跨页更新冲突、切换另一项目后的迟到回调不写入；390px无横向溢出，浏览器异常为空。
- 生产预览保持 http://127.0.0.1:3107 。未访问用户个人浏览器存储/目录，未写实际输出文件，未修改受保护资料。

## Follow-up · 固定顶部与多模型接入评估 · 2026-09-08

- Goal：九个阶段和项目总览固定标题与常用操作，只有下方具体内容滚动；每阶段顶部有明确的下一页入口。说明多模型交叉验证的现状，并只读评估用户提供的LiteLLM目录。
- Non-goals：不接真实模型/API/后端，不安装或运行LiteLLM，不修改Skill、样例与参考资料；不提前实现C—E主体业务。
- Done when：桌面、窄屏与横屏滚动内容时标题/决定/检查/下一步保持可见；各阶段下一步指向正确页面，最后一步返回总览；导航不改变完成状态或绕过业务门槛。切换阶段内容从顶部显示，弹窗、键盘操作和本地保存正常。评估明确区分已有历史报告、预留设计与待实现能力，提供本地证据和后续建议。
- Scope：AppFrame、Workspace、常用操作、局部样式、浏览器回归、PRD与评估说明；业务存储和Service保持现状。
- Verify：npm run typecheck、npm run lint、npm run test、npm run build、npm run test:e2e；隔离浏览器滚动位置/键盘/逐页导航/手机尺寸验证；独立只读复审。
- Risks/assumptions：固定区采用紧凑标题和按钮，项目详细信息、说明和任务进度放到下方，避免长标题或手机横屏挤掉功能内容。LiteLLM仅评估未来服务端网关，不能替代剧本Skill、任务编排或真人试玩。

### 固定顶部与接入评估验证

- 修改前用隔离浏览器复现：原创蓝图标题滚动900px后从y=167变为y=-733，无独立详情滚动区，无“下一步：正文生成”入口。
- 最终typecheck、lint、build exit 0；单元43/43（8.49秒）；针对入口和滚动的浏览器场景15/15；完整浏览器42/42（28.8秒）。
- 新增6个场景覆盖1440／960／390px和844×390横屏：键盘与鼠标滚动、固定区位置不变、弹窗修改与焦点恢复、同页保存保持位置、切页归零、九阶段逐页下一步、导航不写完成状态。1440／390px与横屏滚动截图已查看。
- 独立只读复审Ready，无Blocker／Important／Suggestion。独立验证320×568、568×320、960×390、1440×320均无横向溢出，最小横屏仍保留127px内容区；未保存路径草稿在修改决定后保持、滚动位置保持600px；首页正常滚动，重新进入工作台归零，pageerror为空。
- LiteLLM只读检查与官方文档核验完成，补充需求文件04和PRD第13节：未来适合服务端统一模型接入，当前不安装、不调用真实模型。历史双审、补充设计与待开发范围分别说明。
- 生产预览已恢复 http://127.0.0.1:3107 ，没有改变Service、存储、受保护资料或用户个人浏览器数据。

## Follow-up · GitHub私有仓库、同步与后端计划 · 2026-09-08

- Goal：在当前登录账号w93139建立私有项目仓库，保存已开发代码、数据结构、Mock、测试与计划；提供每轮开发后和手动同步方式。将LiteLLM列入后端实施计划，列明当前人工验收。
- Non-goals：不把GitHub用作数据库，不上传浏览器项目、目录引用、密钥、原始资料或完整第三方工程；不接真实模型、不安装LiteLLM、不开发C—E主体功能，不开启未经选择的后台文件监听。
- Done when：私有远程建立且首次版本与本地一致；同步成功可核对版本，重复同步不产生空提交，失败保留本地数据，远程领先不覆盖；范围排除生效，脚本有实际Git仓库测试与独立审查。计划区分前端C—E和真实后端任务，验收清单面向普通用户。
- Scope：根README／AGENTS／.gitignore、scripts同步工具及测试、Mac入口、01需求目录05—07说明、PRD追加。初始化本地Git及新远程，不修改应用业务文件和存储。
- Verify：Python语法与unittest；隔离本地bare仓库测试同步、重复、失败与远程冲突、危险文件和私有范围保护；拟上传文件清单与常见凭证检查；首次push后gh核对private及远程HEAD，再克隆到临时目录验证文件齐备；独立只读复审。
- Risks/assumptions：用户“后端数据”当前实际只有类型、Mock和前端服务接口；已经询问范围和同步时机，先采用保守的私有代码仓库和每轮验证后同步，不声称用户已回复或浏览器数据已备份。仓库中含必要演示故事摘要，完整原材料保持本机。

### GitHub同步与计划验证

- 真实远程已创建：w93139/juben-workbench，Private，默认main；本地使用账号noreply邮箱，认证由GitHub CLI密钥环处理，无密钥写入仓库。
- Python编译检查、Mac入口zsh语法、git diff --cached --check通过；最终8项隔离bare Git集成测试通过（5.996秒）。首次、重复、第二次更新、只检查不写、推送失败重试、远程领先保护、私有目标与push地址、范围和凭证、未推送历史均覆盖。
- 独立审查发现push.followTags用户配置会附带本地标签；新增回归先失败，再以--no-follow-tags修正。独立复审8/8通过（5.86秒），Ready，无遗留Blocker／Important。
- 首次暂存88项，常见凭证与敏感文件检查通过。git check-ignore验证agent-blueprint、完整原参考目录、依赖、构建和测试产物被排除；应用业务和浏览器数据未修改。
- 初始版本bf298a9647eeca2b544f1d8c56a18a222887631b已实际推送；远程只有main，没有额外标签。重新从GitHub克隆到独立临时目录，88个文件哈希全部一致，排除目录未出现，Mock可解析，Mac入口可执行；临时目录已清理。
- GitHub同步规则、手动入口、LiteLLM所在F3后端计划和5项人工验收清单已写入仓库。浏览器创作状态与真实数据库未备份，不能以代码同步冒充。

## Current contract · 五步工作台精简 · 2026-09-08

- Goal：按用户已确认方案将九个前台入口整合为准备材料、确定创作方案、设计故事、生成与检查、试玩与导出；减少重复设置与研究表单，更新现有PRD并同步私有仓库。
- Non-goals：不接后端或真实模型，不修改受保护参考资料；不把C—E未开发能力冒充完成，不删专业结构、来源、验证类别和受众隔离，不清空旧项目。
- Done when：五步导航和组合页面可用，九个旧地址仍能访问相应内容；顶部保持固定主操作、决定和单一检查入口，重复右栏与全局研究按钮移除；输出目录以项目共用设置和简短位置呈现，阶段目录高级调整仍可用。第二步集中编辑方向/机制，推荐明确暂定，预设体验比例，提前保存草稿不确认，集中确认原子提交且保留失败/冲突草稿。蓝图检查入口及先导出未试玩包的流程说明保留。现有本地数据、只读、材料变化失效与导入恢复不回退。
- Scope：五步展示映射、AppFrame/Workspace/操作与输出设置、组合档案页、研究方案与必要Service、回归测试、PRD/README/验收清单。保留九个底层StageId及已有输出子目录，不迁移LocalStorage格式。
- Verify：typecheck、lint、unit、build、e2e；五步与旧地址、集中草稿/确认、失败原子性/冲突、预设百分比、目录取消/失败/刷新/跨页、固定顶部与窄屏；独立只读复审；同步GitHub并核对HEAD。重建前停原3107预览，完成后恢复同一地址。
- Assumptions：本轮实施已批准的界面及B交互精简；C—E主体仍按后续批次开发。合并生成/审查与试玩/导出时展示同组内容及真实未开发状态，不新增假生成按钮。

### 五步精简 · 实施与验证结果 · 2026-09-08

- 已实施五个可见步骤、旧九个地址兼容、单列正文与固定顶部操作；项目列表同步五步名称。移除各页重复研究入口、右侧重复检查与总览重复决定表单，来源与输出设置按需展开。
- 创作方案合并方向、体验预设、机制取舍、主要风险和折叠分析。研究前可保存完整方案草稿；集中确认通过一次版本校验与一次写入提交，推荐本身不代表保存或确认。保持 schemaVersion 3 与原存储键，未改写旧项目数据。
- 项目总输出目录共用、八个历史子目录仍在高级设置中可达；选择目录仍要求保存后生效，不写入实际文件。失败演示移入“演示选项”。
- 现有 PRD 保留文件名并更新为 v0.2，验收清单与两个 README 同步；LiteLLM 和后端计划按五步位置更新。C—E 主体能力仍待开发，本轮未宣称真实 OCR、生成、检查或导出完成。
- `npm run typecheck`、`npm run lint`、`npm run test`（46/46）通过；最终生产构建通过。浏览器套件共 47 项：整套最后一次运行 46 项通过，发现移动菜单首项越界；修复移动导航的默认居中位移后，涉及导航、工作台、弹窗和固定滚动的 29 项定向回归全部通过，其余 18 项沿用本轮已通过结果。所有失败已闭环，无未解决运行失败。
- 首轮两项拆解门槛断言因测试误把默认展开的依据折叠而失败，已修正测试路径，门槛本身未放宽；移动菜单问题为真实布局缺陷，已修复，补充“准备材料”完整位于视口并实际点击的断言。检查涵盖 1440/960/390 宽度及 844×390 横屏、保存失败/跨页冲突、旧地址/数据、自动导入、原生目录引用保存和刷新、推荐不自动确认。
- 截图人工核对桌面固定顶部及手机五步菜单。目录测试继续使用一次性独立浏览器目录和真实 IndexedDB，仅替换系统选择窗口返回值；未操作个人浏览器或声称完成真实系统窗口验收。
- 独立只读审查：初审指出主要风险隐藏过深，已显示所选机制首要风险并恢复人数/时长提醒；确认数量状态也已统一。复审 Ready，无 Blocker/Important。移动导航增量复审 Ready。
- 本机预览已恢复 `http://127.0.0.1:3107`，项目页响应 200；受保护参考目录和 Skill 未改动。同步沿用私有仓库安全脚本，完成状态以脚本远端 SHA 核验为准。用户 A 阶段验收结论保留，未把本轮 B 阶段标记为用户已验收。

## Current contract · C 故事蓝图编辑 · 2026-09-08

- Goal：五步中的“设计故事”从只读档案变成可编辑蓝图，保存草稿/版本，查看关联与本地结构问题；显示开发批次与原型总体进度，交付到需要作者体验编辑的验收点。
- Non-goals：不接真实AI/OCR/后端，不实施D正文生成双模型审查或E导出试玩录入；不修改只读资料、Skill或浏览器真实数据；不把静态结构提示当作AI审查或真人体验结论。
- Done when：空白与演示副本可建立蓝图，原样例只读；真相、角色、关系、因果时间线、知识发放、线索结论、轮次、主持触发与终局可编辑保存；关联使用实体编号保存并以中文名称展示；草稿允许未完成，检查准确定位缺项/断链；建立版本后再编辑标记待更新并保留历史快照；失败/过期写入不丢输入；旧项目无蓝图字段仍可读且不静默写盘。
- Scope：blueprint domain/service/mock fixture与测试、蓝图编辑组件、工作台与生成审查的版本说明、开发进度展示、PRD/验收/README/TASK。
- Verify：typecheck、lint、unit、build、受影响浏览器流程（桌面/手机、空白/副本/只读、修改关联/版本/刷新/冲突/失败），独立只读审查后同步GitHub。
- Assumptions：原型总进度按A—E五个开发交付等权计算，当前2/5，C交付3/5；单独标注用户验收状态，不冒充真实产品完成度。使用应用自有Mock样例，并明确未提供的主持/终局细节为作者待补充事项。

### C阶段 · 交付与验证结果 · 2026-09-08

- 已交付：显式建立空白/演示蓝图；故事真相及9类记录按6组编辑；角色关系图、基于实体名称的关联选择；字段缺项、因果循环/断链、必要结论的获取条件提示与分页定位；新增/移除/撤销；完整草稿保存；命名版本和独立只读历史浏览；审查准备入口。草稿与最新版本不同会提示重新建立版本，D双审未冒充已运行。
- 旧项目增加可缺省的 blueprint 字段，保持 schemaVersion 3，读取旧数据不写盘；原样例只读，副本须明确载入。只从已有应用Mock转成编辑草稿，未知因果、事件映射、发放条件、主持细节与终局不自动补造。历史检查仍绑定旧资料。
- 用户界面显示开发批次与前端原型总进度：A/B/C=3/5=60%，C待用户测试；B开发完成但没有额外记录用户验收。总览和项目列表读取当前已保存蓝图；旧样例与当前蓝图状态区分。私密线索不再被统计为“公共材料”。
- 版本采用最多20份独立快照，超过上限报错并保留已有内容。不能把未保存输入直接建为版本；已保存但不完整的设计可保留待修订快照。字段与关联检查不判断自然语言推理、全部时间矛盾或信息泄漏，不继承原134项脚本结果。
- 验证：`npm run test` 全量6个文件56项通过；最终 `npm run typecheck`、`npm run lint`、`npm run build` 通过。浏览器首轮52项中49项通过，3项在预填textarea的精确标签定位失败；改用useId/htmlFor分离标签，保持原有输入保留断言不变。新增进度/定位与历史第2角色/选择保持断言后，蓝图、工作台、固定滚动3个测试文件共26项全部通过；其余27项沿用本轮已通过结果，总计53项场景覆盖，无未闭环失败。
- 浏览器覆盖：空白全部结构录入并达到0基础缺项、演示角色修改与关系同步、保存刷新、版本隔离和只读、任意历史角色浏览、删除保留关联及撤销、quota失败与跨页冲突重试、开发进度弹窗及问题定位，桌面1440/960、手机390与横屏844×390。截图人工检查桌面与手机布局。原生目录引用测试仍为隔离浏览器边界替身，没有读取个人资料或代替用户系统窗口体验。
- 独立只读审查：蓝图Service专项10/10；初审建议修正私密线索统计、历史版本任意记录浏览以及关联选择保持，均已修复并补浏览器回归，增量复审Ready，无遗留Blocker/Important/Suggestion。
- PRD沿用原文件名更新为v0.3，当前验收清单与两个README同步C阶段及60%范围。保留A已验收结论，不把开发检查当用户验收。交付停在C的作者体验节点，D/E及后端F仍待开发。
- 本机预览已恢复 `http://127.0.0.1:3107`，蓝图页面响应200；GitHub同步预检99个受控文件通过。受保护目录、真实材料与个人浏览器数据未改动；最终同步以脚本远端SHA核验结果为准。

## Current contract · 开源保护与交互修正 · 2026-09-08

- Goal：按用户明确授权将指定GitHub仓库开源，公开前检查完整历史和当前快照并建立凭证保护；在项目总览提供明确改名按钮，修复输出目录入口/兼容提示，给阶段内容添加克制渐进动效。
- Non-goals：不接真实模型，不存放API密钥，不改变受保护资料，不把浏览器不支持的目录权限伪装成已授权，不重写Git历史或强推；不声称零泄露风险。
- Done when：公开内容与历史扫描完成，代码许可/安全说明清楚；同步允许指定public目标且保持凭证/范围检查，GitHub可用的密钥扫描和推送保护开启并核验；改名保存与刷新正确，原样例继续只读；目录错误可诊断且提供可用下一步；切页动效只作用正文，尊重减少动态效果偏好，不因保存重新播放或重置草稿。
- Verify：同步脚本测试与完整历史检查、适用typecheck/lint/unit/build/浏览器回归、独立只读审查；准备完成后再执行已授权的公开，核对远端可见性、保护设置与提交SHA。
- Risk：公开包含仓库已跟踪文件及提交历史；代码许可与创作素材权利分开，真实资料和Skill不在仓库。前端以后不得携带真实凭证，服务器部署环境和用户浏览器能力仍需分别处理。

### 开源保护与交互修正 · 验证结果

- 总览增加“修改剧本名称”；保存及刷新沿用版本冲突保护，原样例提供创建副本入口。正文切页200ms淡入并轻移，固定操作区不动，同页保存不重播，系统减少动态效果时停用。
- 输出目录提前区分只读、浏览器不支持与非安全地址，补充独立浏览器及Finder复制路径指引。支持时仍由点击直接触发系统选择器，保留真实目录引用及手动路径两种保存方式。尚未收到用户浏览器和具体错误，不能声称已复现其本机问题；自动化只替代原生选择窗口边界，未实际打开个人系统目录窗口。
- 单元56/56通过，最终lint与生产build通过（build包含TypeScript检查）。浏览器全量56项首轮55通过，新增改名场景因未等待异步保存便刷新失败；补充等待弹窗关闭及新标题显示后，最终涉及改名、输出目录、动效、工作台的26项定向回归全部通过，其余30项沿用本轮全量通过结果，无未闭环失败。
- 同步与扫描Python15/15通过；独立只读复审Ready，无Blocker/Important。扫描工作区105文件、4提交、147历史文本对象、5本地引用无规则内发现；远程refs核对仅main，指向已扫描a06ad49。公开前继续由同步脚本检查最终快照与完整历史，不将规则检查描述为绝对安全。
- 代码MIT、故事素材权利边界、服务端API保管计划和PR可信基线扫描已记录；CI是上传后检查，工作流自身变更须人工审查，不能代替本地检查。GitHub实际公开和原生保护状态在操作后另行记录。
- 已推送d0c7d30后执行用户授权的公开；GitHub API回读visibility=public，secret_scanning及secret_scanning_push_protection均enabled，private-vulnerability-reporting为true。LICENSE保留标准MIT正文，授权范围单独放NOTICE，避免GitHub无法识别许可证。生产预览已恢复3107并响应200；原型开发仍为C交付、3/5=60%，D/E与真实API未提前标记完成。

## Current contract · 项目导航与目录简化 · 2026-09-08

- Goal：左侧只列项目，项目顶部展示五步创作流程和清晰改名入口；导入直接选文件夹，输出选择成功即保存；全站绿色调整为暖纸白、墨灰与赭红。
- Non-goals：不接真实后端、OCR、导出或文件系统写入，不修改原始样例/受保护资料，不迁移浏览器数据。
- Done when：项目之间切换和高亮准确，改名同步侧栏，原样例可从改名入口创建副本；五步与旧九地址兼容且固定顶部，窄屏可用；文件夹选择取消保留原设置，保存失败保留选择并可重试，窗口等待期间冲突/切页不覆盖数据；默认不显示路径和阶段子目录表单；支持减少动态效果。
- Scope：AppFrame、Workspace、目录设置和材料导入组件、配色与响应式布局、相关浏览器测试和PRD/验收说明。数据Service接口与历史子目录保存格式保持兼容。
- Verify：typecheck、lint、unit、build、全量浏览器及目录/跨页冲突定向测试；桌面/手机/横屏截图核对，独立只读审查；受控扫描同步GitHub并恢复3107预览。
- Assumptions：目录选择使用浏览器原生权限能力，不伪造不支持环境的输出授权；默认选完即保存，不额外确认。只有保存失败/冲突时保留明确重试。导入依旧是选后自动模拟登记，输出仍只记录位置。

### 项目导航与目录简化 · 交付与验证

- 左侧从阶段列表改为真实项目清单，当前项目高亮，新建入口保留；项目顶部包含总览与五步流程，旧九地址仍兼容。总览与步骤页的剧本名称旁都有“改名”，保存同步侧栏；原始样例的同名入口先创建副本。
- 导入主要按钮为“导入文件夹”，选后沿用自动模拟处理，练习包和格式说明折叠。输出直接选目录并自动保存，普通流程无路径/子目录表单和第二次保存；备用完整路径按需展开，历史自定义子目录保留。保存失败保留目录选择、明确重试；打开窗口前固定版本，跨页冲突与离开后的迟到回调不覆盖项目。
- 全站绿色替换为暖纸白、墨灰与赭红，关系图一并调整；固定顶部与正文渐入保留。桌面1440、手机390、横屏844×390截图已实际查看，标题旁改名、项目清单、流程栏及目录入口可见，正文独立滚动。动效中截图曾过早取帧，最终截图禁用拍摄时动画后核对完整内容，无产品透明度问题。
- 验证：typecheck、lint、单元56/56、生产build通过；全量浏览器61/61通过（37.9秒）。最后修正“仅展开备用方式后取消选择误报未保存”的状态提示并补断言，最终lint/build以及目录、异常恢复和交互16项定向回归全部通过（14.3秒）。无未闭环失败。
- 独立只读审查与增量复审均Ready，无遗留Blocker/Important/Suggestion。范围和凭证扫描通过：105文件、6提交、175历史文本对象、5引用；未改变Service、数据格式、依赖或受保护资料。目录自动化使用独立临时profile和真实IndexedDB引用，仅替换系统picker边界，仍需用户实际体验原生窗口。
- PRD、验收清单和README同步。预览恢复http://127.0.0.1:3107，GitHub通过受控同步脚本核对最终远端SHA。开发仍为C交付待体验、原型60%，没有把交互修正计为D/E开发或用户验收完成。

## Current contract · 炭灰主色与图标改名 · 2026-09-08

- Goal：主色固定为#333333，配暖白#F7F5F1、暖灰#EEECE8、边框#DDD8D1和少量赭色#A17C60；主要按钮与当前导航使用炭灰。标题旁改名仅显示小笔，保留可访问名称、悬停提示和32px点击区域。
- Scope：全局配色、改名按钮/只读样例入口、当前PRD与验收文案；不改业务、目录、数据和既有导航结构。
- Done/Verify：生产构建及lint通过，现有改名/键盘/窄屏/固定顶部浏览器回归通过，桌面手机截图核对；独立只读审查后受控同步GitHub。只读样例仍创建副本，保存行为不变。

- 交付验证：最终lint、生产build（含TypeScript）通过；现有交互/工作台/固定滚动27项浏览器回归全部通过（10.9秒），覆盖改名保存刷新、冲突、键盘焦点和1440/390/844×390布局。桌面及手机截图已查看，小笔无可见文字，主操作为炭灰。独立只读复审Ready，无Blocker/Important；git diff --check通过。没有新增业务测试或变更数据，PRD与验收说明同步，3107预览已恢复。

## Current contract · 样例操作直达 · 2026-09-08

- Goal：修复截图中原始样例的小笔/输出目录按钮跳去新建表单的问题。小笔直接打开名称编辑，保存时自动建立已改名副本；目录按钮直接触发系统picker，选好后一次保存副本与目录，停留同一步骤。
- Non-goals：不修改原始样例与受保护资料，不接真实文件写入或后台服务，不伪造浏览器目录能力；不改变用户已有项目的版本冲突保护。
- Done when：只读入口也执行其标注动作，取消或失败不产生空副本；选择器保留用户点击激活；新项目及目录原子保存，失败可重试且不重复创建；切页后迟到结果不跳回旧项目；名称/目录刷新保留且原样例独立。
- Scope/Verify：ProjectService.create可选初始输出设置与共享校验，EditProject/OutputSettings/Workspace入口及文案，相关单元与浏览器场景，PRD；typecheck/lint/unit/build/E2E和独立只读审查，最终GitHub同步及3107预览。

### 样例操作直达 · 交付与验证

- 旧3107生产预览实际复现：demo-names小笔与“选择输出文件夹”都是href=/projects/new?template=demo的链接。现在两个入口均为按钮；小笔打开名称弹窗，保存时自动新建已改名副本；目录先打开picker，选好后将副本和设置一次写入。导航保留原阶段，原始样例不变。
- create增加可选initialOutput参数，复用目录格式/真实引用校验，校验或写入失败不会留下空项目。已有项目继续使用revision保护。组件按项目/步骤卸载并检查mounted，迟到的目录选择不创建或强制导航。#333333与小笔图标视觉保持。
- typecheck、lint、生产build通过；单元58/58通过（8.37秒），新增2项覆盖创建及初始输出的原子性；完整浏览器67/67通过（40秒），新增6项覆盖样例总览/步骤改名、取消、quota失败重试、目录用户激活/引用恢复、各类失败、迟到结果和原样例独立。审查前修正步骤页剧本名的测试定位（该处是链接而非h1），不改变产品语义。
- 独立只读代码审查Ready，无Blocker/Important；审查者独立目录单元6/6与diff检查通过。Chrome临时标签实查小笔直接弹窗；随后目录请求返回“已取消”，未跳页或创建副本，检查标签已关闭。未观察完整OS选择窗口；成功目录路径的自动化使用隔离profile、真实OPFS/IndexedDB，仅替代原生picker边界。
- PRD、验收说明和应用README已同步，3107预览恢复。受保护资料及已有用户数据不变；按受控脚本同步GitHub，开发仍为C待体验、60%。

## Current contract · D 生成与检查 · 2026-09-08

- Goal：用户已确认本轮测试无问题并继续下一阶段；交付按蓝图版本分模块模拟生成、正文编辑与历史版本、双模型模拟意见、作者取舍与复查，停在D阶段用户体验点。
- Non-goals：不接真实模型/OCR/后端，不实现E导出/试玩录入，不改受保护资料和Skill，不把模板正文或预设意见说成真实AI效果，不把当前测试反馈扩大为B正式验收。
- Done when：选择已保存蓝图版本，六类材料按受众生成；状态自动推进，可取消/失败重试，保存刷新保留；正文修改产生新版、旧版只读；蓝图和正文双审固定资料快照，两侧独立状态，保留部分成功，可重试失败侧，一次互审后分歧仍待作者决定；处理建议先预览并由作者编辑保存，不采纳或暂定留下原因，采纳不冒充问题解决；内容变更后明确待复查。静态字段关联提示、模拟双审、历史检查、真人试玩分别展示。
- Scope：production领域类型/Mock模板/Service操作、第四步工作台、相关顶部与蓝图状态文案、单元和端到端测试、PRD与验收/进度说明。兼容schemaVersion3，新增可缺省字段，不迁移或清空用户数据。
- Verify：typecheck、lint、unit、build及全量E2E，补生成/版本/受众/取消/恢复/双审/分歧/保存失败与失效覆盖，桌面和窄屏查看；独立只读审查后受控同步GitHub并恢复3107预览。
- Assumptions：模板生成是按用户已保存蓝图组织草稿，未知内容保留待补充；模拟任务仅项目打开时推进，可刷新续跑。D交付按A—E五批等权4/5=80%，不包含真实产品后端或用户验收比例。用户本次反馈记C及最近入口修正测试通过，B未额外确认。

### D阶段 · 交付与验证结果 · 2026-09-08

- 已交付六类模板模拟生成、生成任务自动推进／取消／失败重试／刷新恢复、分角色与轮次的玩家材料、主持与终局分区、正文编辑前后预览和独立历史版本。未知与缺少发放条件的内容不会擅自发给玩家；角色本是结构模板，公开关系等叙事组织可后续改善，不宣称正文原创质量已验证。
- 蓝图／正文共用版本化模拟审查：固定蓝图快照与正文ID清单，分别推进两侧状态、失败或取消后单侧重试、预设共同意见和分歧、一次模拟互审、作者采纳／暂定／不采纳及理由。采纳不自动解决，正文经作者编辑成新版后复查；蓝图建议回到已有编辑器处理。草稿、版本与正文变化均明确旧审查需复查，不冒充真实Skill／模型或真人结论。
- Project增加可缺省production字段，保持schemaVersion3，旧数据读取不落盘。沿用Service单次事务写入、revision与只读保护，正文版本240／生成任务40／审查30上限明确拒绝新增，不静默清理历史。任务启动锁定计划目录，取消／失败重试不改变原计划；不写出文件。
- fresh验证：全量单元71/71（7文件，8.52秒）；最终lint及生产build（含TypeScript）通过；最终全量E2E76/76（1.0分钟）通过。新增D的9项浏览器场景包括六类受众投影、刷新续跑、取消与失败重试、单侧意见保留与一次互审、决定刷新、修改前后预览、旧版隔离与复查、草稿已变时审查旧版失效、1440/390宽度、quota保存失败与跨页冲突保留输入、runner暂停恢复与新任务不继承旧暂停。
- 调试闭环：首轮D浏览器4/7通过，修复选择范围的精确可访问名称，另两处为测试过早刷新和选项／状态文字重名；补等待任务已保存运行状态和明确标签定位，D9/9通过。首轮全量72/76通过，四项均为新增h2与页面标题重复，内容标题改“选择创作底稿”后保留旧导航断言，最终76/76通过。runner绑定任务ID时TypeScript发现null访问，修正后最终构建通过；该次构建未完成导致的E2E服务未启动已重新完整执行，无遗留运行失败。
- 桌面生成页及手机审查结果截图已实际查看：#333333、暖白、固定顶部、正文独立滚动和两侧状态可读，无横向溢出。独立只读审查及增量审查Ready，无Blocker/Important；审查者独立production Service13/13与diff检查通过。
- 非阻断恢复细节：若配额写入失败后先取消、再重试同一个任务，旧暂停提示可能仍要求点击一次“继续保存生成与检查进度”；新建任务不受旧暂停影响，资料不丢失。普通失败重试、直接恢复和跨页冲突均已有覆盖，后续可简化此特殊路径。
- PRD升v0.4（沿用文件名），验收清单、开发进度、README、LiteLLM和后端计划同步。C按用户本次反馈记测试通过，B未冒记正式验收；D交付后4/5=80%，E及真实后端F待开发。预览http://127.0.0.1:3107恢复，第四步响应200；受保护资料、Skill与个人浏览器数据未改动。完成后使用受控脚本同步指定GitHub仓库，最终SHA以脚本核验为准。

## Current contract · 完整剧本文件夹作为创作入口 · 2026-09-08

- Goal：按用户纠正，移除正式流程的空白／演示副本选择，直接选择要改写的完整剧本文件夹，自动建立项目并登记材料；随后查看拆解架构与原创大纲、写作方向选项，采用后进入故事设计。
- Non-goals：不实现真实OCR／音视频转写／模型分析，不把文件名或Mock预设当作读过正文；不删除已有项目、原始档案或历史记录，不变更Skill和受保护资料；不继续E开发。
- Done when：首页／新建／旧样例入口导向文件夹上传，无二选一或重复新建表单；文件夹名默认命名，取消、全部无效、保存失败不创建空项目，重试原子保存且不重复；现有项目导入仍在本项目中。材料显示尚未识别，框架维度真实待分析，通用Mock大纲方向不冒充从材料得出的结论；选择保存可刷新恢复，源材料变化使建议过期，显式采用大纲建蓝图且不覆盖已有草稿。
- Scope：创建入口／项目导航／只读档案入口、SourceImport和材料界面、folder-plan领域Mock与Service、第二步建议组件与版本失效、PRD和验收、现有端到端测试适配新入口。保留旧schemaVersion3及历史读取。
- Verify：Service原子创建与失效等单元、typecheck/lint/build、全量浏览器（新文件夹导入及取消/失败/恢复、旧功能使用隔离测试fixture），桌面手机截图、独立只读审查；受控GitHub同步与3107预览恢复。
- Assumption：用户确定的是正式产品输入流程；当前仍严格遵守Mock前端范围。模板大纲是可点击的流程占位，不是对上传内容完成分析。现有开发进度80%，本轮为方向纠正，D未获新验收结论。

### 文件夹入口调整 · 交付与验证结果 · 2026-09-08

- 首页和侧栏加号直接请求完整剧本目录，兼容旧新建地址的上传页；取消空白／演示副本选择及重复命名表单。文件夹名默认作为项目名，材料与项目一次写入；取消、无效输入和保存失败不留下空项目，已有项目补充材料不另建项目。原始档案仅历史只读，已有创作项目保留。
- 新材料项目进入架构拆解和三个通用大纲方向；选择可保存刷新，材料版本变化使旧建议失效。采用只建立故事简介与大纲草稿，人物、真相、事件与线索保留待创作，已有蓝图不覆盖。未读取正文、未做真实OCR或模型分析，页面及草稿均标明通用Mock。旧空白项目已有方向草稿继续使用旧编辑页。
- FolderPlan保持可缺省字段及schemaVersion3；开始时锁定计划输出目录，后改输出设置不改历史计划。目录仅记录位置，未写出文件。普通项目的历史练习说明／筛选项已隐藏，检查与试玩说明按是否有历史内容区分。
- 最终fresh验证：typecheck、lint、生产build及git diff --check通过；单元78/78（8文件，8.44秒）；完整浏览器77/77（1.1分钟）。覆盖目录选取边界、自动建立与取消／失败重试／离开页面、相对路径与音视频元数据、选择刷新及大纲采用、旧方向草稿兼容，并回归故事编辑、目录设置、生成双审、冲突和响应式操作。
- 测试迁移：旧创建表单和自动样例副本已按新产品要求移除，相应入口断言替换；已有功能通过Schema校验的隔离浏览器夹具继续回归，没有新增产品测试后门或清理用户数据。首轮70/76通过后修正文案、等待保存时序与目录取消测试方式，针对复验19/19；中途应构建更新而主动中断的测试不计通过，最终以上述77/77为准。
- 实际查看最新桌面上传入口与窄屏方向卡截图：主色#333333、顶部固定、内容可读；自动化验证浏览器filechooser请求和临时测试目录，不声称人工操作过用户电脑原生选择窗口。测试文件正文哨兵未进入保存状态，未访问个人剧本内容。
- 独立只读审查及最终增量审查Ready，无Blocker/Important；审查者独立7/7文件夹单元与差异检查通过。三项初审文案建议已修正。PRD更新v0.5、README与当前验收清单同步。
- 3107生产预览已恢复，新入口HTTP200。开发仍为80%，本轮是创作入口方向纠正，不冒记D用户验收或E完成。检查后按受控脚本同步指定GitHub；仅代码、Mock、测试与计划，不上传真实剧本或浏览器创作数据，远程版本以脚本核验结果为准。

## Current contract · 对话式方案与交叉验证、分类导出 · 2026-09-08

- Goal：修正输出目录选定后的名称反馈与跨阶段共享；材料输入和成果输出避免混淆；第二步拆解→方向→上下文讨论→方案蓝图，第三步以整体调整和影响预览为主，第四步由主Agent统筹两路模拟独审／互审／核对并在对话中选择修订，第五步分类或全部导出。
- Non-goals：不接真实OCR、模型、API或文件上传服务，不读取用户新上传正文、不覆盖原剧本；原交叉验证目录只读参考工作方式，不把历史结果当当前结果；不删除历史项目或原有细节编辑能力。
- Done when：输出选择成功立刻显示所选名称并保存刷新/跨步骤一致，取消不伪造成功；材料页输入明确且补充入口收起。第二步对话保存上下文并给可编辑、可选择的方案，显式采用输出蓝图；第三步默认整体调整、变更影响与保存前预览，细项折叠。第四步两路结果和主Agent核对单独可见，对话带问题上下文、修改方案与用户选择，不自动宣称问题解决。第五步按资源类下载与全包下载，玩家和主持分隔，真实试玩状态准确。
- Scope：目录UI/Service、folder-plan与对话领域/Mock、蓝图页面、production协调审查、导出Service/页面、相应单元与浏览器回归、PRD与开发/验收说明。
- Verify：typecheck、lint、单元、build、完整E2E；验证名称保存与失败、对话与选择刷新、修改影响/旧版本保护、双审独立结果和作者决策、分类导出内容隔离；桌面/窄屏截图、独立只读审查、受控同步与3107预览。
- Assumptions：用户本轮授权完善五步原型；真正AI交流仍为明确标注的本地模拟。导出可下载浏览器内已有文本包，目录选择不等于授予直接写盘权限，文件由浏览器下载到其下载位置。完整E批验收含真人试玩录入，若本轮仅导出不冒记E全部完成。

### 对话式五步迭代 · 交付与验证 · 2026-09-08

- 输出设置缩成共享条，选中引用返回后显示待保存目录名，成功显示明确已保存位置；总览选择后五步及刷新均保留。取消仍保留原设置，不把原剧本输入目录充当成果输出。截图显示“已取消”，本轮未复现用户电脑原生窗口中“成功返回目录却未保存”；已通过隔离浏览器真实IndexedDB／OPFS引用验证保存边界，原生选择窗口用替代边界、不访问个人目录。已发可选澄清，未将未答复记为确认。
- 已有材料下的补充入口默认折叠，输入与输出说明区分；SourceSupplement保持组件和本批进度，首次导入完成不会因材料数量变化自动折叠。旧研究项目可显式启用新方案对话，保留原研究和已有蓝图。
- 第二步先模拟拆解、选方向，再以材料版本／方向／方案为上下文保存用户消息和模拟回复。独立通用结构覆盖大纲、核心因果、角色、关系、知识、结论、线索、轮次、触发和终局；不读取上传正文，也不是Skill执行结果。用户编辑大纲与真相，保存并采用同一蓝图。重新拆解、切换方向及修改前留方案历史，30快照／80消息上限拒绝新增、不静默删除。
- 第三步默认整体大纲、核心真相、角色目标与选择、轮次节奏；细项折叠保留。修改前后与影响范围用中文展示，不暴露JSON；仅提示关联复核，未做语义自动修复。未保存整体稿时父层显示未保存、锁住版本发布和详细编辑；反向亦然，防止两份输入覆盖。历史版本和编号关系保留。
- 第四步只读参考原交叉验证说明、双向复核与最终报告，提炼固定资料→独审→相互核对→主Agent按证据整理→作者修订→新版本复查，未复制历史问题或通过记录。两路结果、双向复核与主Agent核对独立呈现；问题上下文内发送、编辑方案、保存版本、采纳／暂定／不采纳和理由均持久化。采纳不自动改稿或宣称问题解决；过期资料只读。
- 第五步六类ZIP及全部ZIP真实浏览器下载，仅收录所选蓝图版本最新正文。Markdown与说明／版本／检查清单，空正文禁导出；玩家按角色、主持及终局含谜底分区。全项目原始角色ID形成唯一目录映射，避免字符清洗、大小写、长前缀、Unicode规范化混放。ZIP store／CRC／UTF8与目录结构测试通过，无新增依赖；未实现Word/PDF转换、向所选目录直接写入或真人试玩录入。
- 独立审查发现并修复3 Important：整体稿dirty未联动、第二步覆盖作者方案、角色目录清洗碰撞；复审Ready，定向17/17通过。浏览器首轮83/86：发现审查初建时空findings使选中值未跟随后到结果，修为有效已选或首问题推导；另整体角色textarea控件显式aria-label以稳定可访问名称。针对4/4通过，最终增量只读审查Ready，无Blocker/Important/Suggestion。
- 最终验证：lint、typecheck、生产build、diff检查通过；全量单元88/88（10文件，8.47秒）；最终全量浏览器86/86（1.2分钟）。覆盖上下文保存、方案切换历史、整体调整前后与脏稿互斥、两路协调与旧资料只读、真实分类／总包下载、角色隔离、目录名称五步共享与保存失败重试、既有功能回归。已查看桌面目录名称／方案对话，以及窄屏整体修改预览、审查对话和导出截图。
- PRD升v0.6（沿用文件名），README与五步验收清单同步。A—D已完整交付、E仅导出部分，本轮未冒记用户验收或E全部完成，进度仍80%。3107生产预览恢复，入口HTTP200。按受控脚本同步代码、Mock、测试和计划，真实资料、Skill、个人浏览器项目和目录引用不改、不推送；SHA以脚本核验结果为准。

## Current contract · 四步正式流程与本机文件能力 · 2026-09-09

- Goal：按用户最新要求改为四步，上传→拆解及方向→关键蓝图调整→主Agent协调交叉验证及导出。移除顶部下一步/决定/检查按钮和大段模拟说明；唯一主操作右下角。输出位置缩为名称与更换，修复本机选取依赖浏览器取消的问题。
- Scope：四步工作台与旧链接兼容；真实上传读取／自动适用OCR、本机目录选择／保存／打开的窄服务；模型Service与配置缺失状态；绑定材料及蓝图版本的真实模型分析／生成／交叉验证调用边界；PRD和验收、测试、安全审查。
- Done when：第一步只上传与文件状态、读取完可拆解；第二步只显示真实拆解与建议并可生成蓝图；第三步保留大纲真相角色节奏并补关系／关键证据／终局关键节点，细节按需；第四步默认状态图、失败与需处理事项，不显示假通过，验证通过可导出完整档案并打开保存位置；用户所选目录名称保存跨步骤。
- Non-goals：不抹除已有用户项目或历史，不改只读Skill及参考资料，不将旧Mock内容冒充本次读取或模型结果，不外泄输入／密钥，不默许未答复的模型配置与收费调用。不接入未经授权的远程服务。真人试玩不能被模型通过替代。
- Verify：四步/按钮/旧链接/目录取消与成功/文件读取与OCR/无配置阻断/模型编排及过期保护/下载安全的单元和浏览器测试，构建与lint/typecheck，独立审查；测试材料自建，运行文件与输入不入GitHub。
- Assumption：本次明确要求真实读取与自动处理，取代旧只登记的范围；已提出模型调用来源的必要澄清，同时完成不依赖配置的工作。本机辅助仅回环同源可用，不能作为互联网部署方案。

### 四步真实流程 · 实施与验证 · 2026-09-09

- 已替换为四步导航和底部唯一主要操作，移除顶部旧操作组及大模拟说明。输出共享位置缩一行，系统选好即显示名称并保存，跨阶段/刷新一致。旧项目不清空，列表可下载旧版JSON；原旧编辑界面不继续挂载，新正文需真实重新读取。
- 首页文件读取完成后建立项目，文件对象/提取正文/已建项目ID用于失败重试，首次进入材料页不二次要求上传。新正文、蓝图、审查与版本独立存IndexedDB；材料失败不以名称冒充读取完成。音视频未转写会阻止继续，用户可明确排除。
- 本机PDFKit/Vision/textutil实际读取；目录原生桥接、服务端通过快照ZIP写入与Finder打开。本机接口发现并修复Next内部localhost/浏览器127.0.0.1规范化差异，保持双端回环、同端口、Origin与实际Host绑定，忽略转发头。实际生产端点文本读取200，恶意Host/端口/Origin/跨站及无Origin变更403。
- 主模型设计门→六类完整材料→两独审→两互审→主复核，真实模型适配器严格校验内容与引用。未配置503零外呼，不能生成Mock答案。先持久UUID再外呼，同ID幂等恢复；蓝图编辑固定起始revision，材料/方向/创作要求变化不能恢复旧审查资格。
- 类型检查、ESLint、构建通过；113项单元通过，含实际自建TXT/UTF16/PDF文本/图片OCR/RTF读取及失败限制、跨站拒绝、任务编排与幂等、快照导出。模型transport测试均使用隔离fixture，没有真实模型外呼。
- 当前四步10项浏览器场景全部通过：目录名称与刷新、真实TXT与首传一次、音频不可伪读、持续轮询、蓝图/审查失败、跨标签页冲突、修改要求失效、存储失败不外呼、导出写入/打开参数、窄屏布局。原五步86条Mock UI场景移到tests/legacy-e2e作历史，不计当前通过；旧底层单元仍运行。
- 桌面及390px窄屏截图已检查，内容区单独滚动，底部按钮可见，无横向溢出。系统目录选择/打开在浏览器测试中替换为边界fixture，未替用户选择私人目录；真实原生窗口仍需人工验收。
- 独立只读审查首轮提出5项Important，全部修复后二审Ready；无新增安全问题。写盘失败仅清理本次独占创建的inode，防残包且不删已有文件。Git忽略检查确认runtime-data和.env.local不提交。
- PRD v0.7、README、边界、后端增量与验收清单同步。四步UI/本机读取可测试；真实三模型联调尚缺服务配置，早前澄清未回答，不视为授权。全文上下文600KB、单文件25MB/30万字、PDF60页等保护明确拒绝超限；大型整本分块、持久任务、上下文聊天、分类/Word/PDF及真人试玩录入继续计划，不报整体产品100%。

## Current contract · 文件夹替换与拖拽入口、模型连接 · 2026-09-09

- Goal：首页红框内容替换为整块拖拽区、中央上传文件夹按钮；上传后项目内显示更改文件夹，可替换选错的整批输入，不混入旧材料；给模型未连接状态可操作的安全设置入口。
- Scope：首页与首阶段共享拖拽选择交互、递归读取拖拽目录、Service整批替换及失效规则、连接设置UI和同源本机配置、PRD/验收及针对性测试。
- Non-goals：不使用Mock冒充真实拆解；不替用户选择付费模型、不搜其他项目凭证、不在浏览器/Git保存密钥、不改原上传文件或只读Skill资料。
- Done when：首次可点击/拖目录，已有材料按钮更名，更换成功仅新材料参与；取消/读取或存储失败保留旧材料与输出位置；材料版本变化使旧分析/蓝图/审查失效；连接状态明确且可保存用户提供的服务配置，未配置不假解锁。
- Verify：目录遍历/替换失效/安全配置单元；首页拖拽UI、换目录取消失败/刷新、连接入口E2E；typecheck/lint/build、独立只读审查与授权GitHub同步。
- Risk：拖拽目录浏览器支持不同，保留原生选目录；真实模型凭证与服务仍需用户输入，已发澄清，未答复不视为授权付费调用。

### 文件夹替换与连接入口 · 交付证据 · 2026-09-09

- 首页原说明卡/最近项目表移除，主体为共享FolderDropzone，中央“上传文件夹”；项目材料已有内容时展示输入目录名称和“更改文件夹”。支持拖拽高亮、全部目录批次递归、相对路径、系统文件过滤、数量/层级上限，选择按钮仍使用原生文件夹选择。
- replaceMaterials在完整读取后CAS整批替换，任何status:error、无成功正文、事务失败拒绝更新；读取取消保留旧批次，提交阶段明确保存并禁用取消。新sourceRevision令旧分析/蓝图/审查失效，不混用旧输入，不改输出位置或原磁盘文件。
- 页面底部增加“配置模型”，可填兼容服务地址、password密钥及三个不同模型编号；只保存不调用模型。受限runtime-data/studio-settings.json原子存储，GET不返回密钥，空key只同地址保留，换URL须新key，环境变量整组优先；版本冲突、symlink、宽松文件权限、超字节配置均拒绝。没有真实服务或密钥输入，未宣称模型已连接。
- typecheck、lint、build通过；121单元、16浏览器场景通过。新增覆盖拖拽递归多于100项、真实目录选择/整批替换、取消和HTTP/单项OCR混合失败、保存期间禁取消、刷新保持、旧结果失效、模型设置保存后刷新且不外呼/不持久保存浏览器secret；既有四步场景通过。
- 首轮独立审查提出混合error替换与保存期间取消两项Important，修复后二审Ready。浏览器测试首次失败为Playwright对directory input不接受[]模拟取消，改派发浏览器cancel事件后通过，另实际读取Abort与延迟IDB保存边界有独立覆盖。
- 已检查首页桌面/390px窄屏、换本与配置窗口截图；无横向溢出。模型窗口用测试网络边界，实际配置保存单元使用自建临时目录，不污染用户配置或调用付费模型。
- PRD升v0.8并同步验收与README。拖拽API技术依据链接置README。同步扫描曾命中单元中虚构的URL内嵌用户名密码；测试改为运行时URL对象构造占位值，不含实际凭证，完整扫描复查通过。

## Current contract · 蚂蚁平台自动选型与费用保护 · 2026-09-09

- Goal：连接蚂蚁数科 MaaS 后先免费读取当前账号可用模型，再用固定合成题进行一次有上限的真实测评，自动分配主创作模型和两路审查模型；用户剧本不用于选型样题。
- Scope：平台连接与候选发现、公开价解析、固定金标准评分、三角色自动分配、SQLite 费用与任务账本、停止／失败／重启恢复、LiteLLM 运行配置和后续接入说明、PRD与验收界面。
- Done when：保存 Key 不产生模型调用；候选列表只保留账号可用且有可解析公开价的文本模型；测评计划估算不超过8元，单次按公开价上限加20%预留，账本总上限10元；每次发起前重新校验10分钟内价格；异常调用按不确定费用保守记账并停止；结果展示结构、证据、原创、格式、延迟、Token和估算费用，自动分配三个不同模型。
- Non-goals：工作台估算不代表蚂蚁平台最终账单，不替代平台侧消费限额；本批不自动启动LiteLLM代理，不读取其他项目凭证，不把Key写入浏览器、Git或日志；未由用户点击开始测评前不进行任何付费调用。
- Verify：设置与锁、候选发现、评分抗投机、费用原子预留与不确定费用、跨进程任务所有权、取消、价格过期、模型响应校验、持久恢复和LiteLLM配置单元；连接界面E2E；typecheck/lint/build/仓库扫描和独立只读安全审查。

### 中断恢复与模型测评报告 · 交付证据 · 2026-09-09

- 已确认本机记录为3/12道可评分题：Qwen3.8-Max完成三题并保存64分；已核算¥0.20；两次未形成可用正文的调用共¥0.04暂列待平台核对。旧版没有保存异常响应原文，不能事后断言具体缺失字段，也不把待核对金额写成已扣款。
- 响应适配现支持字符串或明确`type: text`的正文分片；推理分片不作为最终答案。正文截断、空正文或选项不兼容时结算可核算费用、排除该候选并由用户决定是否免费复核价格后换入候选。实际模型编号缺失或不匹配时无法确认计费身份，按最高预留列待核对并禁止自动续测。
- 续测最多三次且每次都需用户点击；完成题目不重复调用，排除候选不再进入保留候选冲突，累计排除与部分题记录容量覆盖四批候选。当前旧记录会推断并跳过连续中断的下一候选，但不补造该候选的单项费用或响应内容。
- 新增确定性Markdown测评报告：完整、部分、首题失败和取消均可查看/下载；列出角色分配、候选状态、完整汇总、部分逐题记录、排除原因、已核算/预留/待核对费用及适用边界。报告读取不调用模型；页面仅展示与当前记录版本一致的报告。
- fresh验证：完整单元159/159、当前E2E 17/17、typecheck、lint、webpack生产构建及diff检查通过。测试均使用隔离fixture，没有读取API Key、用户剧本或发起真实付费调用。独立审查提出候选替换、旧费用归属、失败报告、模型身份计价、文本分片、报告版本及容量问题，均已修复并以新增回归覆盖。

### 蚂蚁平台自动选型 · 交付与验证 · 2026-09-09

- 设置分为平台连接、免费读取候选、受限真实测评三步。服务器地址预填蚂蚁官方兼容地址；Key只在本机受限文件中保存，页面读取不回传，密码框保存后清空。候选发现先用Key查询账号可用模型，再与蚂蚁公开目录及价格交集，不把较高价格当作质量结论。
- 测评使用三组确定性合成样题，覆盖故事结构与证据摘录、原创方向和一致性审查。返回必须为JSON、模型编号一致、完整结束且含Token用量；低于结构、证据、原创或格式门槛的模型不得进入自动分配。主创作和两路审查分别按职责加权，质量接近时优先费用较低者。
- SQLite账本以整数分记录已用、预留和不确定费用，事务阻止并发超额；任务租约避免两个服务进程重复发起同一批调用。每次调用前检查公开价格是否仍在10分钟内，发生超时、取消或响应不明确时停止后续调用并保守记入不确定费用。内部计划上限8元、逐次20%预留、账本硬上限10元均为公开价估算，真实账单仍以蚂蚁平台为准，建议同时设置平台侧消费限额。
- LiteLLM代理依赖固定为已发布稳定版`litellm[proxy]==1.100.0`，锁文件解析116个包；可生成不含密钥的三个角色别名配置。当前正式调用仍由工作台直连蚂蚁平台，代理自动启动、健康检查、恢复和切换留在下一后端批次，界面未伪装为已经经过LiteLLM。
- 最终fresh验证：ESLint、TypeScript、生产构建和diff检查通过；23个单元文件138/138通过；当前E2E 16/16通过；`uv lock --check`通过；仓库扫描通过。独立只读审查最终Ready，无Blocker/Important，重点复核费用并发、任务租约、重启取消、密钥边界和每次调用前的价格时效。
- 验证期间没有读取用户Key、没有连接真实模型、没有产生付费调用。开发侧本批已完成，真实账号候选、公开价和模型质量需要用户在配置页粘贴Key后依次执行免费发现与受限测评；在此之前不宣称模型已选定或真实流程已通过。
- 首次真人连接时，重启后的新接口已成功保存4个候选模型，但交接前未刷新的旧前端不认识新增的价格检查／更新时间字段，页面显示`unrecognized_keys`。候选读取本身成功且费用为0；刷新可恢复。评测视图现改为允许忽略未来新增的展示字段，避免后续兼容性中断；定向单元8/8、模型相关浏览器3/3、typecheck、lint与生产build通过，全程未开始付费测评。
- 首次真实小样在完成Qwen三题后，LingDT返回正文但未返回标准Token用量；费用保护在3/12停止，记录已核算0.20元、待核对0.02元，未继续扣费。后续修复保留已有得分：完全缺用量时按请求／响应字节上界与最高预留核算；部分、非法或超预留用量禁止续测；一般中断由用户点击后免费复核价格并只补缺题，最多一次。每题结果与结算同一SQLite事务，续测资格以跨进程版本CAS取得，有费用后禁止清空重读候选。真实续测仍待用户在最新页面主动启动。
- 续测修复最终验证：23个单元文件148/148、当前E2E 17/17、ESLint、TypeScript、生产构建与diff检查通过；独立复审Ready，无Blocker/Important。E2E中的“未配置模型”用例已隔离模拟连接状态，避免读取本机真实配置造成误报，不清空或改写用户Key、候选和费用记录。测试与修复过程没有继续真实付费调用。

## Current contract · 模型返回兼容与测评报告 · 2026-09-09

- Goal：一次解释并修复真实测评第二次停在3/12的问题；保留已完成成绩和费用记录，在用户再次主动操作后只补未完成题目；结束时生成可查看、可下载的中文Markdown模型测评报告。
- Non-goals：不读取或展示API Key，不自动发起付费调用，不清空或伪造现有Qwen成绩／费用，不降低既定质量门槛，不把固定合成小样写成真实剧本或真人试玩验证，不把模型选型报告混入作品交叉验证报告。
- Done when：OpenAI兼容返回中缺省模型编号、非stop结束、字符串／文本分片正文可安全提取并按实际格式表现评分；无可提取choice、模型编号明确不符、异常Token和超预算仍停止。最多三次人工续测，价格与预算每次复核，跨进程不重复。已有题目不重测。已产生质量结果后可查看／下载报告，部分测评不排名、不伪造缺失明细，费用与最新SQLite视图一致且不含凭证、用户材料或本机路径。
- Scope：测评领域状态、响应适配、续测CAS与费用保护、确定性报告生成/API/配置窗口、单元及E2E、PRD/README。保持当前数据库向后兼容，不迁移或删除运行记录。
- Verify：覆盖响应变体、不可恢复结构、重复续测/跨进程/预算、旧汇总无逐题明细、完成/未达标/中断报告、特殊字符与敏感字段排除、下载不外呼；运行相关单元、全量单元、E2E、lint、typecheck、build、diff/凭证扫描和独立只读审查。真实付费续测留给用户在更新页面点击。
- Risk：当前异常响应原文未落盘，因此只能确定是顶层/choice/完成标志/正文类型之一不符合原严格schema；修复按可验证的兼容边界处理，不把无法归属或费用异常的响应强行采用。既有记录没有开始时间、逐题详情和失败模型归属，报告如实标未知。
