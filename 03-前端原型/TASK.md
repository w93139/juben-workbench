# Task：阶段B · 参考研究与原创方向

> 当前任务：A 已获用户验收，本批交付 B 阶段。用户授权在无需额外决定时持续推进开发；普通实现与检查不再逐项申请确认。

## Current contract · 阶段 B · 2026-09-08

- Goal：从本机项目完成“模拟上传／识别 → 校对与保留材料缺口 → 有来源的模拟拆解 → 机制取舍 → 保存原创方向”，刷新后恢复。
- Non-goals：不接真实OCR/AI/后端；不把本机选择的文件冒充已识别，不把机制摘录冒充完整参考本；不修改受保护资料或重审已验证Skill。
- Done when：可载入来源明确的内置研究练习材料，登记本机文件元信息；任务有进度、失败重试、取消与刷新继续；能确认识别问题、排除重复页、明确保留缺失内容；材料变化使旧拆解失效；机制可保留/改造/舍弃并保存理由；人数、题材、时长、体验比例、内容边界和原创思路可保存，风险随目标变化展示；旧A项目能兼容恢复，副本隔离和只读基准仍有效。
- Scope：research领域类型/Mock/Service、四个研究阶段页面、任务状态及阶段进度、数据兼容、测试和PRD/README说明。C—E维持后续开发顺序，开始各批之前补写具体契约；本批不混入其他开发阶段。
- Verify：`npm run typecheck`、`npm run lint`、`npm run test`、`npm run build`、`npm run test:e2e`；新旧数据、失败/取消/过期更新、阶段完整链路、移动端；独立只读审查。
- Assumptions：沿用唯一完整原创样例《名字之外》。OCR异常为明确标识的练习数据；参考拆解使用已有机制研究摘录，客观真相缺失处保持未知。原始参考页面未导入，不捏造阅读覆盖或识别准确率。

## Goal

用户能创建空项目或《名字之外》演示副本，查看完整阶段导航、项目总览和结构数据摘要，编辑项目备注与决定状态，并在刷新后恢复。

## Non-goals

- 不实现真实OCR、模型、后端、账号、队列或部署。
- 不提前实现蓝图编辑器、正文生成、审查修复、试玩表单和导出服务。
- 不修改任何参考资料或Skill；不把历史检查视为当前操作的验证。

## Done when

- 首页／项目列表、新建页面、项目工作台与阶段摘要可访问，桌面与窄屏可用。
- 《名字之外》基准只读；演示副本与空项目能独立保存、编辑和恢复。
- 项目标题／备注和决定状态可更新；编辑不改变只读的原始剧本内容。
- 展示真实结构统计、明确的来源、版本、模拟／历史标识和未试玩状态。
- 持久化有版本校验、存储失败提示、损坏恢复入口；未成功保存不显示成功。
- 不同项目隔离，过期版本写入被拒绝，重复点击有进行中保护。
- 所有业务访问经过Service接口；JSON／Markdown与组件分离。
- 下列验证完成并经过独立只读审查。

## Scope

- 新增：../01-产品需求/03-简易PRD-v0.1.md。
- 应用：当前目录内的app、components、domain、services、mocks、tests与构建配置。
- 更新：当前README（保留原目录用途并增加运行、边界与验证说明）。
- Preserve：../02-演示与测试资料、../agent-blueprint及外部原创项目。

## Verify

- `npm run typecheck` — 类型正确。
- `npm run lint` — 代码检查。
- `npm run test` — Service、数据校验、基准隔离、版本冲突、保存失败与恢复。
- `npm run build` — 生产构建。
- `npm run test:e2e` — 创建／复制／编辑／刷新／阶段导航／存储异常／窄屏。
- 浏览器：检查1440px与390px布局、键盘表单、保存提示、历史与试玩标签。
- 独立reviewer读取任务契约、代码和上述实际结果，修复Blocker／Important并复审。

## Risks and assumptions

- 用户已确认新手优先、仅一套完整示例、正文自由编辑后置。
- 当前是浏览器本机状态，不能跨设备同步，也不提供真实玩家账户隔离。
- 示例版本缺少明确日期时显示“原始样例”，不捏造历史修改日期。
- 保存的项目元信息与底稿分开；底稿只读，只有副本备注／决定状态可修改。
- 依赖版本在当前可用Node.js环境中确认并锁定。

## Verification results · 2026-09-07

- `npm run typecheck`：exit 0。
- `npm run lint`：exit 0，无警告。
- `npm run test`：15 / 15 通过。
- `npm run build`：exit 0，首页、项目列表、新建、项目总览及阶段路由构建成功。
- `npm run test:e2e`：10 / 10 通过（最终运行 9.8 秒）。覆盖契约流程、坏数据备份恢复、拒绝保存、多页面版本冲突、长备注与后台读失败时草稿保留。
- 桌面 1440px、手机 390px：布局人工看图检查完成，手机无横向溢出，浏览器 pageerror 为空。
- 来源核验：11 份源数据哈希、3 份 Markdown 副本与参考目录一致。
- 独立只读审查：Ready，无遗留 Blocker / Important。修复后台读取失败导致草稿卸载的问题；补全明确的冲突覆盖提示及弹窗关闭焦点返回，经过独立浏览器复核。

本次验证是前端功能验证，不是对剧本体验、真实AI或真人试玩的验证。

## Follow-up · 2026-09-08 · 找得到决定与检查入口

- Goal：用户在项目总览和任一阶段的首屏即可找到决定状态及检查记录，无需猜测它们在右侧还是页面下方。
- Non-goals：不推进 B 阶段，不改变剧本数据、历史审查结论、LocalStorage 格式或只读规则。
- Done when：可编辑项目顶部有“修改决定状态”，只读样例有“查看创作决定”及创建副本说明；顶部始终有“检查与试玩”；点击即可打开对应面板，状态选择有明确标签，修改后刷新保留；弹窗可关闭并返回触发按钮焦点。
- Scope：workspace-actions 新组件、workspace / workspace-panels、少量样式、浏览器回归场景及 README 操作说明。复用已有数据与 Service，不添加依赖。
- Verify：先用新增浏览器测试复现入口缺失；修复后运行 `npm run typecheck`、`npm run lint`、`npm run test`、`npm run build`、`npm run test:e2e`，覆盖 1440 / 960 / 390px 与只读／副本／阶段页面；独立只读审查。
- Risks：原位置的面板和弹窗复用同一状态；弹窗中的操作应正确保存且不修改基准。测试在隔离浏览器进行，不动用户本机项目。

### Follow-up verification results

- 复现：新增 960px 测试在旧版本失败，首屏“修改决定状态”入口不存在；代码确认 <=1020px 时右侧区域移至主体内容之后，原始样例的决定只显示标签。
- 修复：项目标题下新增两个常用操作，弹窗复用现有面板；状态下拉框有“修改状态”标签，只读样例说明复制后才能修改。
- 最终 `npm run typecheck` / `npm run lint` / `npm run build`：exit 0；`npm run test`：15 / 15；`npm run test:e2e`：14 / 14（8.0 秒）。
- 新增 4 个浏览器场景：1440 / 960 / 390px 全按钮首屏可见、弹窗修改与刷新保留、阶段页面直接操作、关闭焦点返回、只读复制入口。
- 实际截图查看：960px 首屏入口、390px 决定及检查弹窗均可读。
- 独立只读复审：Ready，无遗留问题；额外验证弹窗保存失败保留旧值、恢复后重试、原面板同步及跨阶段状态，pageerror 为空。
- 预览已在原地址 http://127.0.0.1:3107 重新启动；用户项目、数据格式和受保护资料未改动。


## 阶段 B 验证记录 · 2026-09-08

- 新增独立 research 类型、来源可追溯的JSON、Service操作和四个阶段界面；真实文件只登记元信息，OCR问题是人工练习数据。
- 旧格式1读取不写，首次成功保存升级为格式2；仍使用原存储键。只读样例、其他项目和保护目录不变。
- 类型检查、lint、生产构建通过；单元测试22/22。完整浏览器回归19/19（23.0秒），覆盖A回归、B桌面/390px完整链路、失败重试/取消/刷新继续、文件登记保存失败、多页草稿保护与材料变化失效。
- 桌面1440px、手机390px截图已查看，无横向溢出；完整B浏览器链路pageerror为空。
- 独立审查发现并已修复：未编辑的旧表单可能覆盖他页最新字段。现在无本地修改时同步最新保存，有草稿时保留输入并检查版本冲突；四类研究表单均使用同一保护。
- 已完成但随后失效的拆解任务明确标为“上次完成，当前待重新拆解”，不再展示100%进度误导。
- 独立复审结果：Ready，无遗留Blocker/Important。审查者独立浏览器复核两种跨页编辑顺序，并核对3份摘录、8条机制与来源哈希。
- 原地址 http://127.0.0.1:3107 已启动新生产预览。验证通过不等同于用户已验收B，更不等同于真实OCR/AI或真人试玩验证。

## Follow-up · 2026-09-08 · 找得到参考研究入口

- Goal：用户从原始样例、可编辑项目总览或任一阶段，均可找到“继续参考研究”，顺利进入材料中心并载入练习包。
- Non-goals：不开发C阶段、不改OCR/研究业务或存储格式、不自动创建项目或修改只读样例，不变更受保护资料。
- Done when：项目顶部始终有研究快捷入口；已有项目直达材料中心且保留进度；只读样例点击研究入口或材料页载入操作，明确进入副本创建，创建成功后直达材料中心；未创建时不写本机数据。已载入时明确显示已有状态和下一步。桌面/窄屏、A/B既有功能继续可用。
- Scope：workspace-actions/workspace、research/materials、create-project及新建路由、少量入口说明、README/PRD与浏览器回归。
- Verify：先在旧生产预览复现缺失入口；新增e2e在旧构建失败；修复后运行typecheck/lint/test/build/test:e2e，检查桌面和390px、原始样例/副本/空项目、自动进入材料中心及刷新状态；独立只读复审。
- Risks/assumptions：只能确认当前生产版本的显示条件，无法直接读取用户当前浏览器（CUA管道不可用）；浏览器验证使用隔离Playwright存储，保持预览地址3107与用户数据不变。新建路由只接受固定的research意图，不接受任意跳转地址。


### 参考研究入口修复验证

- 旧生产复现：原始样例总览和蓝图页无“继续参考研究”；只读材料页“载入研究练习包”为禁用按钮。新增1440px入口测试在旧构建失败。
- 最终typecheck、lint、build exit0；单元22/22；浏览器24/24（21.1秒）。1440/960/390px研究入口和创建后载入按钮完整在首屏；新建失败保留输入、重试只创建一份；既有研究状态刷新与跨阶段返回保留。
- 修正原有刷新测试的时序：等待任务保存后出现“取消任务”再刷新，避免刷新抢在重试请求持久化之前。
- 截图已查看；独立只读复审Ready，无遗留问题。审查者在隔离390px浏览器验证只读材料→创建失败→重试成功→载入→返回研究保留，pageerror为空。
- 预览已更新并保持原地址 http://127.0.0.1:3107 ，用户本机项目和所有受保护资料保持不变。

## Follow-up · 多格式材料与文件夹登记 · 2026-09-08

- Goal：整文件夹（含子文件夹）和多种文件可登记；选后先看清数量、分类、相对路径、重复/不支持文件及大文件提示，再批量登记。清单可筛选分页，刷新后保留。
- Non-goals：不接真实OCR、音视频转写、视频抽帧、文件上传、压缩包解压；不读取文件正文或媒体内容，不修改原文件。不开发C阶段。
- Done when：文件/文件夹双入口；保留目录相对路径，不同目录同名文件不互相覆盖；图片/PDF/文档/音频/视频/字幕/压缩包分类及待处理方式明确；单批2000份、单项目5000份作为可配置的原型保护，取消单文件30MB硬限制；大文件可登记但提示后续拆分处理。零字节、隐藏文件、不支持格式、疑似重复在预览列出原因，可登记其余有效项；未提交不写入。存储满/冲突保留待登记清单，失败重试不重复；旧A/B数据无损恢复。
- Scope：独立source-import类型/策略/检查、research Schema与登记Service、source-import组件、材料清单筛选分页、必要存储格式3兼容、测试与PRD/README说明。策略阈值是本产品原型建议，不是第三方OCR能力承诺。
- Verify：typecheck/lint/test/build/test:e2e；真实测试文件夹递归/同名文件/多格式/超10份和超30MB/部分略过/去重/分页/失败重试/旧格式升级/只读隔离；独立只读审查。预览维持3107，隔离浏览器测试。
- Risks/assumptions：只保存元信息，因此文件本身不占浏览器存储，但大量元信息仍可能存储满，必须保留原数据与本次选择。文件夹选择受浏览器/系统文件选择器支持限制，不支持时明确提示多选文件；路径仅保留所选文件夹内相对路径。音视频格式分类基于扩展名，不声称验证了编码可解码；压缩包只登记待解包。重复判断基于相对路径/大小/可用修改时间，无内容哈希，标为疑似重复。

### 多格式材料与文件夹登记验证

- 最终 `npm run typecheck`、`npm run lint`、`npm run build`：exit 0；`npm run test`：31/31；`npm run test:e2e`：27/27（32.6秒）。
- 隔离浏览器实际选择递归测试文件夹：42份中39份可登记，3份明确略过；包含不同目录同名页、300MB PDF、3GB视频、音频、字幕和压缩包。大文件使用稀疏测试文件，只验证元信息登记，不读取或处理内容。
- 覆盖预览不写、取消/取消勾选、失败重试、重复预览、搜索筛选和分页、刷新保留及只读样例；Service验证2000份单批和5000份项目边界。1440px与390px截图已查看，无横向溢出。
- 独立审查发现旧格式中反斜杠/换行文件名会被新输入规则误判损坏。新增回归先复现失败，再分开新导入校验与持久化名称兼容，最终通过；旧格式1/2读取不改存储，成功写入才升级3。
- 独立只读复审：Ready，无遗留Blocker/Important。审查者用隔离浏览器复核旧名称原样保留、跨页冲突、载入最新状态仍保留勾选、重试只登记选中项；浏览器异常为空。
- 新生产预览已恢复原地址 http://127.0.0.1:3107 。本次未接入真实文件处理，未修改用户本机项目、Skill或受保护资料。

## Follow-up · 自动导入进度与输出路径 · 2026-09-08

- Goal：选择文件或文件夹后自动开始模拟上传、展示进度、自动保存并显示结果，无需再点登记或完成；拆解及后续阶段可设置和查看输出位置。
- Non-goals：不接真实上传/OCR/AI，不读取文件正文，不创建本机目录或写出剧本；不开发C—E的主体编辑生成能力，不变更受保护资料。
- Done when：自动分类并略过无效/疑似重复项，保存成功后才能到100%；支持取消、失败后保留清单并重试，数量限制和保存冲突不会造成部分写入；离开页面会取消未完成导入。页面明确标注模拟进度。项目总输出目录与拆解/机制/方向/蓝图/正文/审查/试玩/成品子目录可设置、刷新保留、项目隔离；完整路径预览、未设置提示和只读保护明确；拆解任务记录启动时的计划输出位置，改路径不冒充已移动旧结果。兼容旧项目，读取不改原存储。
- Scope：导入进度事件与Service接口、Mock进度适配、导入组件、输出路径类型/设置组件/Service、拆解任务路径快照、相关测试与README/PRD；复用现有保存/版本冲突策略，不加依赖。
- Verify：typecheck、lint、test、build、test:e2e；浏览器桌面/390px自动完成、混合目录、大文件、取消与失败重试；Service原子保存、冲突和旧格式读取；路径保存/跨阶段/只读/异常输入/失败与冲突、拆解启动快照；独立只读复审。
- Risks/assumptions：用户所说的上传仍属已确认的本地Mock范围，因此进度不表示网络字节传输。输出路径当前只保存计划，浏览器不能凭输入路径直接写本机；未来接后端/授权目录写入时再实际使用。自动导入替代上一轮手动勾选确认；取消只在提交前有效，刷新/离开后需重新选择未导入批次。

### 自动导入与路径设置验证

- 最终typecheck、lint、build：exit 0；单元39/39（8.40秒）；浏览器30/30（22.2秒）。
- 自动导入覆盖递归目录、300MB PDF/3GB视频元信息、无效项和疑似重复自动略过、无确认自动保存、100%仅在保存后、保存失败清单保留、取消与重试、SPA离开取消、只读基准；Service覆盖等待存储锁时取消、跨页冲突及重试不覆盖原记录。
- 输出路径覆盖默认未设置、macOS/Windows拼接、总目录跨阶段共用及子目录独立、刷新保留、旧项目读时不写、只读与项目隔离、非法路径、写入失败和跨页草稿保护；拆解任务启动路径快照不随后续设置改变。
- 1440px与390px浏览器截图已查看，窄屏无横向溢出，相关场景pageerror为空。独立审查指出POSIX目录中的反斜杠可能误触发Windows分隔符；已改为按盘符/UNC前缀判断并补回归。
- 预览已更新并保持 http://127.0.0.1:3107 。用户本机数据未被测试访问，未写真实上传/输出文件，受保护资料保持不变。
- 独立只读复审Ready，无遗留Blocker/Important/Suggestion。独立隔离浏览器确认95%等待锁时离开不提交、跨页冲突后重试仅添加一次且保留另一页备注、旧项目读时不写、路径草稿不静默覆盖；390px无横向溢出，浏览器异常为空。

## Follow-up · 材料下一步与交互选择输出文件夹 · 2026-09-08

- Goal：材料中心顶部下一步进入参考本拆解，不再自跳转；输出路径可通过系统目录选择窗口选取（请求从桌面打开），并保存选中目录和阶段子目录。
- Non-goals：不绕过拆解审计门槛，不接OCR/上传/生成/真实输出，不扫描/修改所选文件夹内容，不建立原生后端，不修改受保护资料。
- Done when：材料页显示“下一步：参考本拆解”，点击正确导航；未审计可浏览下一页但不能开始模拟拆解。其他页的研究入口和只读创建副本保持可用。输出位置提供选择/重新选择文件夹、手动路径；选后预览，保存并刷新保留；取消选择保留原设置，拒绝/不支持/存储失败有可恢复提示，不伪造绝对路径。跨页选择期间发生修改不能静默覆盖，切页后迟到的选择不得写入另一个项目。
- Scope：workspace-actions/workspace、输出设置类型/表单/Service、独立浏览器目录适配器及引用存储、测试、README/PRD。只新增必要的目录引用IndexedDB存储，不加依赖；旧LocalStorage版本保持可读。
- Verify：先用浏览器复现材料中心自跳转；typecheck/lint/test/build/test:e2e；导航1440/390px、审计门槛、目录选择调用参数/取消/失败/不支持/刷新/同名目录引用隔离/旧路径兼容/冲突草稿保护；独立只读复审。
- Risks/assumptions：showDirectoryPicker由用户点击触发、mode=read、startIn=desktop；起始位置由浏览器决定，不保证只可选择桌面目录。浏览器不返回完整本机路径，因此保存目录引用及名称，预览标注“所选文件夹”；不会把名称当作绝对路径。IndexedDB保存引用，实际写入授权和文件生成仍后置；若浏览器不支持目录选择则保留手动输入。浏览器自动化在隔离存储验证适配器边界，不操控用户个人目录。
- 验证环境调整：本机Playwright无痕上下文读取IndexedDB中的OPFS目录引用会导致浏览器退出，纯API最小复现也失败；独立临时profile的完整Chromium可成功往返。目录浏览器场景改用每测试独立临时profile并在结束后关闭删除；不使用用户个人profile，不修改产品逻辑来绕过验证。

### 材料下一步与交互选择文件夹验证

- 旧生产预览复现：材料中心“继续参考研究”的href与点击后URL均为当前materials页，“下一步：参考本拆解”不存在。
- 最终typecheck、lint、build exit 0；单元43/43（8.42秒），完整浏览器36/36（24.9秒）。新增6个浏览器场景与4个Service测试；1440/390px截图已查看。
- 目录场景使用每测试独立临时profile、真实OPFS目录handle和真实IndexedDB往返，替代的仅为系统picker返回值。验证用户点击激活、startIn桌面/read参数、保存与刷新、取消/拒绝/不支持/存储失败、同名引用及跨页草稿保护；没有实际操作系统原生选择窗口。
- 独立只读复审Ready，无遗留Blocker/Important/Suggestion。独立复核目录引用丢失、保存失败恢复、选择期间跨页更新冲突、切换另一项目后的迟到回调不写入；390px无横向溢出，浏览器异常为空。
- 生产预览保持 http://127.0.0.1:3107 。未访问用户个人浏览器存储/目录，未写实际输出文件，未修改受保护资料。

## Follow-up · 固定顶部与多模型接入评估 · 2026-09-08

- Goal：九个阶段和项目总览固定标题与常用操作，只有下方具体内容滚动；每阶段顶部有明确的下一页入口。说明多模型交叉验证的现状，并只读评估用户提供的LiteLLM目录。
- Non-goals：不接真实模型/API/后端，不安装或运行LiteLLM，不修改Skill、样例与参考资料；不提前实现C—E主体业务。
- Done when：桌面、窄屏与横屏滚动内容时标题/决定/检查/下一步保持可见；各阶段下一步指向正确页面，最后一步返回总览；导航不改变完成状态或绕过业务门槛。切换阶段内容从顶部显示，弹窗、键盘操作和本地保存正常。评估明确区分已有历史报告、预留设计与待实现能力，提供本地证据和后续建议。
- Scope：AppFrame、Workspace、常用操作、局部样式、浏览器回归、PRD与评估说明；业务存储和Service保持现状。
- Verify：npm run typecheck、npm run lint、npm run test、npm run build、npm run test:e2e；隔离浏览器滚动位置/键盘/逐页导航/手机尺寸验证；独立只读复审。
- Risks/assumptions：固定区采用紧凑标题和按钮，项目详细信息、说明和任务进度放到下方，避免长标题或手机横屏挤掉功能内容。LiteLLM仅评估未来服务端网关，不能替代剧本Skill、任务编排或真人试玩。

### 固定顶部与接入评估验证

- 修改前用隔离浏览器复现：原创蓝图标题滚动900px后从y=167变为y=-733，无独立详情滚动区，无“下一步：正文生成”入口。
- 最终typecheck、lint、build exit 0；单元43/43（8.49秒）；针对入口和滚动的浏览器场景15/15；完整浏览器42/42（28.8秒）。
- 新增6个场景覆盖1440／960／390px和844×390横屏：键盘与鼠标滚动、固定区位置不变、弹窗修改与焦点恢复、同页保存保持位置、切页归零、九阶段逐页下一步、导航不写完成状态。1440／390px与横屏滚动截图已查看。
- 独立只读复审Ready，无Blocker／Important／Suggestion。独立验证320×568、568×320、960×390、1440×320均无横向溢出，最小横屏仍保留127px内容区；未保存路径草稿在修改决定后保持、滚动位置保持600px；首页正常滚动，重新进入工作台归零，pageerror为空。
- LiteLLM只读检查与官方文档核验完成，补充需求文件04和PRD第13节：未来适合服务端统一模型接入，当前不安装、不调用真实模型。历史双审、补充设计与待开发范围分别说明。
- 生产预览已恢复 http://127.0.0.1:3107 ，没有改变Service、存储、受保护资料或用户个人浏览器数据。

## Follow-up · GitHub私有仓库、同步与后端计划 · 2026-09-08

- Goal：在当前登录账号w93139建立私有项目仓库，保存已开发代码、数据结构、Mock、测试与计划；提供每轮开发后和手动同步方式。将LiteLLM列入后端实施计划，列明当前人工验收。
- Non-goals：不把GitHub用作数据库，不上传浏览器项目、目录引用、密钥、原始资料或完整第三方工程；不接真实模型、不安装LiteLLM、不开发C—E主体功能，不开启未经选择的后台文件监听。
- Done when：私有远程建立且首次版本与本地一致；同步成功可核对版本，重复同步不产生空提交，失败保留本地数据，远程领先不覆盖；范围排除生效，脚本有实际Git仓库测试与独立审查。计划区分前端C—E和真实后端任务，验收清单面向普通用户。
- Scope：根README／AGENTS／.gitignore、scripts同步工具及测试、Mac入口、01需求目录05—07说明、PRD追加。初始化本地Git及新远程，不修改应用业务文件和存储。
- Verify：Python语法与unittest；隔离本地bare仓库测试同步、重复、失败与远程冲突、危险文件和私有范围保护；拟上传文件清单与常见凭证检查；首次push后gh核对private及远程HEAD，再克隆到临时目录验证文件齐备；独立只读复审。
- Risks/assumptions：用户“后端数据”当前实际只有类型、Mock和前端服务接口；已经询问范围和同步时机，先采用保守的私有代码仓库和每轮验证后同步，不声称用户已回复或浏览器数据已备份。仓库中含必要演示故事摘要，完整原材料保持本机。

### GitHub同步与计划验证

- 真实远程已创建：w93139/juben-workbench，Private，默认main；本地使用账号noreply邮箱，认证由GitHub CLI密钥环处理，无密钥写入仓库。
- Python编译检查、Mac入口zsh语法、git diff --cached --check通过；最终8项隔离bare Git集成测试通过（5.996秒）。首次、重复、第二次更新、只检查不写、推送失败重试、远程领先保护、私有目标与push地址、范围和凭证、未推送历史均覆盖。
- 独立审查发现push.followTags用户配置会附带本地标签；新增回归先失败，再以--no-follow-tags修正。独立复审8/8通过（5.86秒），Ready，无遗留Blocker／Important。
- 首次暂存88项，常见凭证与敏感文件检查通过。git check-ignore验证agent-blueprint、完整原参考目录、依赖、构建和测试产物被排除；应用业务和浏览器数据未修改。
- 初始版本bf298a9647eeca2b544f1d8c56a18a222887631b已实际推送；远程只有main，没有额外标签。重新从GitHub克隆到独立临时目录，88个文件哈希全部一致，排除目录未出现，Mock可解析，Mac入口可执行；临时目录已清理。
- GitHub同步规则、手动入口、LiteLLM所在F3后端计划和5项人工验收清单已写入仓库。浏览器创作状态与真实数据库未备份，不能以代码同步冒充。

## Current contract · 五步工作台精简 · 2026-09-08

- Goal：按用户已确认方案将九个前台入口整合为准备材料、确定创作方案、设计故事、生成与检查、试玩与导出；减少重复设置与研究表单，更新现有PRD并同步私有仓库。
- Non-goals：不接后端或真实模型，不修改受保护参考资料；不把C—E未开发能力冒充完成，不删专业结构、来源、验证类别和受众隔离，不清空旧项目。
- Done when：五步导航和组合页面可用，九个旧地址仍能访问相应内容；顶部保持固定主操作、决定和单一检查入口，重复右栏与全局研究按钮移除；输出目录以项目共用设置和简短位置呈现，阶段目录高级调整仍可用。第二步集中编辑方向/机制，推荐明确暂定，预设体验比例，提前保存草稿不确认，集中确认原子提交且保留失败/冲突草稿。蓝图检查入口及先导出未试玩包的流程说明保留。现有本地数据、只读、材料变化失效与导入恢复不回退。
- Scope：五步展示映射、AppFrame/Workspace/操作与输出设置、组合档案页、研究方案与必要Service、回归测试、PRD/README/验收清单。保留九个底层StageId及已有输出子目录，不迁移LocalStorage格式。
- Verify：typecheck、lint、unit、build、e2e；五步与旧地址、集中草稿/确认、失败原子性/冲突、预设百分比、目录取消/失败/刷新/跨页、固定顶部与窄屏；独立只读复审；同步GitHub并核对HEAD。重建前停原3107预览，完成后恢复同一地址。
- Assumptions：本轮实施已批准的界面及B交互精简；C—E主体仍按后续批次开发。合并生成/审查与试玩/导出时展示同组内容及真实未开发状态，不新增假生成按钮。

### 五步精简 · 实施与验证结果 · 2026-09-08

- 已实施五个可见步骤、旧九个地址兼容、单列正文与固定顶部操作；项目列表同步五步名称。移除各页重复研究入口、右侧重复检查与总览重复决定表单，来源与输出设置按需展开。
- 创作方案合并方向、体验预设、机制取舍、主要风险和折叠分析。研究前可保存完整方案草稿；集中确认通过一次版本校验与一次写入提交，推荐本身不代表保存或确认。保持 schemaVersion 3 与原存储键，未改写旧项目数据。
- 项目总输出目录共用、八个历史子目录仍在高级设置中可达；选择目录仍要求保存后生效，不写入实际文件。失败演示移入“演示选项”。
- 现有 PRD 保留文件名并更新为 v0.2，验收清单与两个 README 同步；LiteLLM 和后端计划按五步位置更新。C—E 主体能力仍待开发，本轮未宣称真实 OCR、生成、检查或导出完成。
- `npm run typecheck`、`npm run lint`、`npm run test`（46/46）通过；最终生产构建通过。浏览器套件共 47 项：整套最后一次运行 46 项通过，发现移动菜单首项越界；修复移动导航的默认居中位移后，涉及导航、工作台、弹窗和固定滚动的 29 项定向回归全部通过，其余 18 项沿用本轮已通过结果。所有失败已闭环，无未解决运行失败。
- 首轮两项拆解门槛断言因测试误把默认展开的依据折叠而失败，已修正测试路径，门槛本身未放宽；移动菜单问题为真实布局缺陷，已修复，补充“准备材料”完整位于视口并实际点击的断言。检查涵盖 1440/960/390 宽度及 844×390 横屏、保存失败/跨页冲突、旧地址/数据、自动导入、原生目录引用保存和刷新、推荐不自动确认。
- 截图人工核对桌面固定顶部及手机五步菜单。目录测试继续使用一次性独立浏览器目录和真实 IndexedDB，仅替换系统选择窗口返回值；未操作个人浏览器或声称完成真实系统窗口验收。
- 独立只读审查：初审指出主要风险隐藏过深，已显示所选机制首要风险并恢复人数/时长提醒；确认数量状态也已统一。复审 Ready，无 Blocker/Important。移动导航增量复审 Ready。
- 本机预览已恢复 `http://127.0.0.1:3107`，项目页响应 200；受保护参考目录和 Skill 未改动。同步沿用私有仓库安全脚本，完成状态以脚本远端 SHA 核验为准。用户 A 阶段验收结论保留，未把本轮 B 阶段标记为用户已验收。

## Current contract · C 故事蓝图编辑 · 2026-09-08

- Goal：五步中的“设计故事”从只读档案变成可编辑蓝图，保存草稿/版本，查看关联与本地结构问题；显示开发批次与原型总体进度，交付到需要作者体验编辑的验收点。
- Non-goals：不接真实AI/OCR/后端，不实施D正文生成双模型审查或E导出试玩录入；不修改只读资料、Skill或浏览器真实数据；不把静态结构提示当作AI审查或真人体验结论。
- Done when：空白与演示副本可建立蓝图，原样例只读；真相、角色、关系、因果时间线、知识发放、线索结论、轮次、主持触发与终局可编辑保存；关联使用实体编号保存并以中文名称展示；草稿允许未完成，检查准确定位缺项/断链；建立版本后再编辑标记待更新并保留历史快照；失败/过期写入不丢输入；旧项目无蓝图字段仍可读且不静默写盘。
- Scope：blueprint domain/service/mock fixture与测试、蓝图编辑组件、工作台与生成审查的版本说明、开发进度展示、PRD/验收/README/TASK。
- Verify：typecheck、lint、unit、build、受影响浏览器流程（桌面/手机、空白/副本/只读、修改关联/版本/刷新/冲突/失败），独立只读审查后同步GitHub。
- Assumptions：原型总进度按A—E五个开发交付等权计算，当前2/5，C交付3/5；单独标注用户验收状态，不冒充真实产品完成度。使用应用自有Mock样例，并明确未提供的主持/终局细节为作者待补充事项。

### C阶段 · 交付与验证结果 · 2026-09-08

- 已交付：显式建立空白/演示蓝图；故事真相及9类记录按6组编辑；角色关系图、基于实体名称的关联选择；字段缺项、因果循环/断链、必要结论的获取条件提示与分页定位；新增/移除/撤销；完整草稿保存；命名版本和独立只读历史浏览；审查准备入口。草稿与最新版本不同会提示重新建立版本，D双审未冒充已运行。
- 旧项目增加可缺省的 blueprint 字段，保持 schemaVersion 3，读取旧数据不写盘；原样例只读，副本须明确载入。只从已有应用Mock转成编辑草稿，未知因果、事件映射、发放条件、主持细节与终局不自动补造。历史检查仍绑定旧资料。
- 用户界面显示开发批次与前端原型总进度：A/B/C=3/5=60%，C待用户测试；B开发完成但没有额外记录用户验收。总览和项目列表读取当前已保存蓝图；旧样例与当前蓝图状态区分。私密线索不再被统计为“公共材料”。
- 版本采用最多20份独立快照，超过上限报错并保留已有内容。不能把未保存输入直接建为版本；已保存但不完整的设计可保留待修订快照。字段与关联检查不判断自然语言推理、全部时间矛盾或信息泄漏，不继承原134项脚本结果。
- 验证：`npm run test` 全量6个文件56项通过；最终 `npm run typecheck`、`npm run lint`、`npm run build` 通过。浏览器首轮52项中49项通过，3项在预填textarea的精确标签定位失败；改用useId/htmlFor分离标签，保持原有输入保留断言不变。新增进度/定位与历史第2角色/选择保持断言后，蓝图、工作台、固定滚动3个测试文件共26项全部通过；其余27项沿用本轮已通过结果，总计53项场景覆盖，无未闭环失败。
- 浏览器覆盖：空白全部结构录入并达到0基础缺项、演示角色修改与关系同步、保存刷新、版本隔离和只读、任意历史角色浏览、删除保留关联及撤销、quota失败与跨页冲突重试、开发进度弹窗及问题定位，桌面1440/960、手机390与横屏844×390。截图人工检查桌面与手机布局。原生目录引用测试仍为隔离浏览器边界替身，没有读取个人资料或代替用户系统窗口体验。
- 独立只读审查：蓝图Service专项10/10；初审建议修正私密线索统计、历史版本任意记录浏览以及关联选择保持，均已修复并补浏览器回归，增量复审Ready，无遗留Blocker/Important/Suggestion。
- PRD沿用原文件名更新为v0.3，当前验收清单与两个README同步C阶段及60%范围。保留A已验收结论，不把开发检查当用户验收。交付停在C的作者体验节点，D/E及后端F仍待开发。
- 本机预览已恢复 `http://127.0.0.1:3107`，蓝图页面响应200；GitHub同步预检99个受控文件通过。受保护目录、真实材料与个人浏览器数据未改动；最终同步以脚本远端SHA核验结果为准。
