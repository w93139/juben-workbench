# 安全与API密钥

当前为本机四步工作台，已接入真实文件读取、macOS OCR、模型兼容接口和本机SQLite测评/任务存储。公开仓库仅保存代码与非敏感模板，真实配置和运行数据留在被Git忽略的本机目录。当前回环访问保护不能替代未来联网服务的账号与项目权限。

## 已设置的仓库防护

- 2026-09-08已核实GitHub仓库为Public，Secret scanning、Push protection及私密漏洞报告均已启用。它们用于发现受支持的疑似密钥，不代替服务端访问控制和密钥管理。
- 默认限制同步目录，排除真实材料、浏览器状态、运行数据、密钥文件和`.env`系列文件（仅允许无真实值的`.env.example`）。
- 同步前检查工作区、暂存区、全部本地引用可达的提交及标签信息；检查疑似凭证、带密码的连接URL、私钥、敏感配置和公开环境变量中的密钥。错误只显示位置，不打印内容。
- `python3 scripts/check-repository.py`是独立只读扫描，不需要GitHub登录，也不提交或推送。扫描完整历史需完整克隆。
- GitHub Actions在push和pull request后运行扫描，权限仅为读取代码，检出后不保留凭证，不使用模型或部署密钥。PR使用目标分支base SHA的扫描器和测试规则检查PR内容，不执行PR提供的扫描脚本；对工作流和规则本身的修改仍须人工审查。CI发生在上传之后，不能代替上传前的本地扫描。
- 自动规则不能识别所有泄露或敏感故事内容，也不保证密钥绝对不会被盗。不要通过强制添加或直接推送绕过检查。

## 当前本机API边界

- 服务仅监听127.0.0.1；本机API校验回环Host/端口、同源Origin及跨站请求标记。尚无用户登录或跨项目账号权限，不用于公网或局域网多用户访问。
- 密钥通过受限runtime-data/studio-settings.json（目录0700、文件0600）、被忽略的.env.local或进程环境供服务端使用。页面保存会短暂持有输入并提交到本机，GET不返回完整密钥；不持久保存到LocalStorage/IndexedDB。环境配置优先，避免混用来源。
- 原始材料读取到本机；用户主动开始AI操作后，正文或当前步骤材料发送到配置的模型供应商。保存配置、刷新页面与查询状态不生成模型内容。
- 小样测评有人民币预留账本和估算限额；正式拆解/蓝图/全文审查尚无独立累计费用保护。创作并发上限2，超限/中断不会自动增加预算或重跑。
- 任务结果、通过快照、分段摘要在本机SQLite保留7天。它们可能包含故事内容，虽不是原始输入文件，也属于用户数据，禁止提交GitHub。

## 联网部署与后续运行保障必须做到

1. 供应商密钥只在服务端使用；本机保存在受限`runtime-data`、被忽略的`.env.local`或进程环境，部署时使用平台Secret管理。只提交空白配置模板，不把真实值发给聊天、Issue或PR。
2. 联网服务中浏览器只能调用本项目经过身份验证的后端接口。密钥不得硬编码进客户端或进入打包产物、LocalStorage、IndexedDB、URL或`NEXT_PUBLIC_`环境变量；本机配置表单的短暂粘贴/提交不等于持久保存。
3. 后端按用户和项目校验权限，限制请求频率、并发、单任务与月度预算，防止公开接口被他人代用；限制模型网关网络入口，不直接暴露供应商或网关主密钥。
4. 测试与生产分开、权限最小化、限制可调用模型；支持撤销与轮换密钥，日志和错误响应隐藏密钥及不必要的用户材料。联网部署前验证这些措施并检查客户端构建产物。
5. GitHub部署如需凭证，优先短期身份或环境Secret，限制可使用的工作流与环境审批；不向不受信任的PR提供Secret。

## 发现疑似泄露

先在供应商端撤销并更换密钥，检查用量和访问记录。删除最新文件或改为私有不能消除历史泄露。暂停同步和公开，评估所有Git历史、缓存、制品及已下载副本，明确范围后再处理历史并恢复发布。

报告问题时不要在公开Issue或PR贴密钥、真实剧本或用户数据。若仓库提供GitHub私密漏洞报告入口，使用该入口；否则只公开说明希望建立私密联系，不披露漏洞或秘密内容。
