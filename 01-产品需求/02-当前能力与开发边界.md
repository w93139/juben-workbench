# 当前能力与开发边界

更新：2026-09-19。当前为本机真实工作流联调阶段。以当前代码和最新TASK记录区分“实现”“开发验证”“真实联调”“用户验收”；不沿用旧Mock阶段的完成结论。

## 当前已经实现

| 部分 | 能力 | 验收边界 |
|---|---|---|
| 四步界面 | 材料、拆解与方向、蓝图、交叉验证与导出；输出目录固定显示 | 系统窗口、原浏览器与实际项目仍需用户验收 |
| 真实材料读取 | 文件夹递归读取；文本、PDF、图片及文档提取；macOS OCR | 音视频转写、压缩包解包未接入；识别正确性需对照原件 |
| 模型连接与选型 | 蚂蚁兼容接口、候选发现、定向小样、人民币测评账本、三模型分配 | 2026-09-09最近记录为9/9完成；只证明当时小样完成，不证明整本效果 |
| 正式创作预算 | 项目累计额度、费用预览、逐次报价/预留/核算、待核对费用与账单核对 | 与测评独立；当前仅蚂蚁官方平价报价；假供应商开发验证，真实整本账单尚待核对 |
| 长原剧本拆解 | 大输入无损分段、分层汇总、可核对引用、进度、成功摘要复用 | 12 MB/2000材料；分段覆盖不等于所有语义关系正确；真实整本待验收 |
| 蓝图与版本 | 生成蓝图；九类节点完整编辑、关联问题定位、12份草稿自动保存/恢复；20份历史对照/恢复新草稿/逐份清理 | 关联检查不代替语义和体验验证；缺失引用保留需人工修复；正式提交后旧审查失效，历史删除前应完整备份 |
| 正文与交叉审查 | 主模型设计门、六类正文生成、两路独审、互审、主模型复核；阶段保存、失败手动继续与完整报告 | 正文按角色/轮次/模块逐份生成保存，最多240份；短篇整体审查，长篇按正文段及关联范围逐路审查、保留完整原文与报告；真实全链待联调；静态问题可在蓝图编辑器修正，模型自由文本意见需作者自行对应 |
| 完整项目备份 | 项目JSON下载、导入预览、新副本恢复，材料/蓝图/历史/草稿/正文/报告/部分阶段成果保留，失败补偿与重启核对 | 64 MiB；仅已保存浏览器数据，历史通过不是新导出资格；原文件、费用账本、模型配置和目录权限分别保管 |
| 本机任务恢复 | SQLite保存任务、结果和通过快照；同UUID恢复不重复调用；分段缓存可手动重试复用 | 结果/快照/缓存7天；不是后台执行队列，进程中断不自动继续生成 |
| 作者记忆 | 本机受限SQLite保存跨项目偏好/文风/禁忌六类条目；手动增删改、启用停用、使用次数；拆解生成原创方向时自动带入启用条目并记录使用 | v1手动库；自动候选提炼与冲突提醒未做；记忆只作参考，不覆盖项目创作要求，也不改变已通过成果；拆解输入指纹随记忆变化，不错误复用旧结果 |
| ZIP输出 | 仅从服务端通过快照导出，玩家按角色与主持分区，避免覆盖同名文件 | Markdown/JSON为主；尚无本次真实成品验收或真人试玩结论 |

9月18日工程整理已完成：修复创作要求保存与三项正文/蓝图关联校验问题，删除不可达旧UI，分离真实服务和模拟流程，补充旧数据迁移保护回归。已有schema和浏览器数据保留。

## 尚需开发

- 长篇分段审查与M5工程交付已完成；同一材料到实际ZIP的路由集成测试和独立浏览器回归通过，真实全案语义验收仍待完成。蓝图及各请求仍限600000字节，长篇首次生成后保存，重新预览实际分段费用后执行。任意非相邻正文语义一致性不由相邻关联检查证明。
- 模型语义意见的结构化字段定位；当前静态问题已可定位，历史可显式整理，仍保留20份上限。
- 作者记忆后续：从项目行为/评语自动提炼候选并人工确认；审查中提示“与此前已否决方向相似”；按需检索与导出/导入。v1已手动可用，未接入自动来源。
- 运行可观测：轨迹JSONL与质量/体验指标面板（覆盖率、问题数、一次性通过率、耗时、费用）尚未接入。
- 长期项目数据库、用户与项目权限、跨设备访问、任务执行队列、云端备份与运行监控。
- LiteLLM代理启动、健康检查和恢复；目前正式调用直连兼容接口。
- 分类导出、Word/PDF、印刷材料、音视频转写及真人试玩记录录入。

## 数据与证据

浏览器LocalStorage保存项目元数据，IndexedDB保存正文/蓝图/审查；本机`runtime-data`保存受限配置、目录登记、SQLite测评/正式创作费用账本和创作任务/摘要缓存/正文检查点。原始输入文件不被修改，GitHub不备份这些用户数据。

历史样例、Mock、旧双模型报告及旧134项成品检查属于方法或历史演示证据，不属于当前新作验收。开发测试使用自有文本和假模型；完整作品的可读性、可玩性与泄密风险仍需人工核查和真人试玩。

下一步按[当前验收清单](06-当前验收清单.md)完成材料与整本拆解验收，再按[开发计划](05-后端开发计划.md)推进正式创作。
